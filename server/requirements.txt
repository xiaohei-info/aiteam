# v1 server 运行期最小依赖。
# 契约层只需 pydantic；三端服务壳与 shared 底座需 fastapi/uvicorn；service_client 需 httpx。
pydantic>=2.11,<3
fastapi>=0.115
# Official MCP Python SDK; FastMCP Streamable HTTP facade.
mcp==1.13.1
uvicorn>=0.30
httpx>=0.27
# 认证：RS256 非对称签名 + JWKS（shared/auth，D23）。
pyjwt>=2.8
cryptography>=42
# Manager 多租户 PG/RLS 底座（shared/db, manager_service，D20/D22）。
psycopg[binary]>=3.1
# 覆盖率闸门：diff-cover 改动分支覆盖≥90%（#234 F1 CI 物理层）+ pytest-cov 分支度量。
diff-cover>=9.2
pytest-cov>=5.0
# agent_service 使用 UploadFile = File(...) 需要 python-multipart
python-multipart>=0.0.20
