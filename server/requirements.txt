# v1 server 运行期最小依赖。
# 契约层只需 pydantic；三端服务壳与 shared 底座需 fastapi/uvicorn；service_client 需 httpx。
pydantic>=2.11,<3
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
# 认证：RS256 非对称签名 + JWKS（shared/auth，D23）。
pyjwt>=2.8
cryptography>=42
# Manager 多租户 PG/RLS 底座（shared/db, manager_service，D20/D22）。
psycopg[binary]>=3.1
# Agent Gateway 接 ACP 协议 runtime（Hermes 等）的官方客户端 SDK（06 §7.2，复用不自造）。
agent-client-protocol>=0.10
