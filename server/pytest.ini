[pytest]
testpaths = tests loops
python_files = test_*.py
markers =
    integration: tests that hit the live test server or external integration surface
    pr_quick: release-blocking subset for PR gate (5-10 min budget, covers loop A/B/C + API/security)
