# multica 并行开发编排 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 `parallel-dev-orchestration-multica` skill：leader(orchestrator) 把设计拆成 issue + 声明式 manifest，固定 Python 引擎读 manifest/metadata 自治执行 DAG，worker/reviewer 经 multica 原生派发并留档。

**Architecture:** 设计见同目录 `2026-06-23-multica并行开发编排-设计.md`（spec，唯一口径）。引擎拆为「纯逻辑（manifest/lint/graph/compile/engine driver）+ IO 边界（multica CLI client）」；纯逻辑用 `FakeMulticaClient` 完全 TDD，与 pilot 无关；真 CLI client 与 executor skill 在 pilot 验证后实现。

**Tech Stack:** Python 3 + pytest（与 `scripts/orchestrator/` 同形态）；multica CLI；YAML manifest。

**分阶段与 gate：**
- **Phase 0（spike，非 TDD）** — pilot 验证 spec §10 两项硬前提，go/no-go 决策门。
- **Phase 1（TDD，pilot 无关）** — 引擎纯逻辑核（manifest/lint/graph/compile/engine driver over FakeMulticaClient）。**本计划详写到此**。
- **Phase 2/3（pilot 后细化）** — 真 multica CLI client + `run_dag.py` 入口；executor skill + SKILL.md + 打包。仅列任务与接口，具体实现待 Phase 0 结论回流（见文末）。

**落点根目录：** `.claude/skills/parallel-dev-orchestration-multica/`（下称 SKILL_DIR）；引擎代码在 `SKILL_DIR/scripts/`。

---

## Phase 0 — Pilot spike（决策门，先做，throwaway）

> 目的：用最小代价回答 spec §10 两个问题。**这是 spike 不是 TDD**——产出是「结论 + 真实 CLI 输出样本」，结论回流后才细化 Phase 2/3。全程用一次性 issue/分支，做完清理。

### Task 0.1：捕获真实 `issue list/runs` JSON 形状

**Files:** Create `SKILL_DIR/pilot/findings.md`（记录结论与样本）

- [ ] **Step 1：建一个 throwaway issue 并打依赖/worker metadata**

```bash
WS=410ade5e-8ae0-4402-b975-813dea2ff3e1   # 替换为目标 workspace
A=$(multica issue create --workspace-id $WS --title "[PILOT] dag node A" --status todo --output json | python3 -c "import sys,json;print(json.load(sys.stdin)['id'])")
B=$(multica issue create --workspace-id $WS --title "[PILOT] dag node B" --status todo --output json | python3 -c "import sys,json;print(json.load(sys.stdin)['id'])")
multica issue metadata set $B blocked_by "[\"$A\"]"
multica issue metadata set $B worker "dev-member-1"
echo "A=$A B=$B" | tee -a SKILL_DIR/pilot/findings.md
```

- [ ] **Step 2：抓 `issue list --output json` 与 `metadata list` 的真实字段，落 findings.md**

```bash
multica issue list --workspace-id $WS --limit 5 --output json > SKILL_DIR/pilot/sample_issue_list.json
multica issue metadata list $B --output json > SKILL_DIR/pilot/sample_metadata.json
```
确认：`status` 取值集合、`metadata` 是否随 list 返回（spec 假设是）、`blocked_by` 存储/读出格式（JSON 串还是数组）。记进 findings.md。

- [ ] **Step 3：清理 throwaway issue**

```bash
multica issue status $A cancelled; multica issue status $B cancelled
```

### Task 0.2：验证「worker agent 产 gateable git 产物」（go/no-go #1）

- [ ] **Step 1：把一个真实成员 agent 指派到一个 throwaway issue，触发 run**

```bash
C=$(multica issue create --workspace-id $WS --title "[PILOT] make a trivial PR" \
    --description "在集成分支基础上新建分支，加一个空文件 pilot_probe.txt，开 PR(base=集成分支)，回报 PR 链接" \
    --status todo --output json | python3 -c "import sys,json;print(json.load(sys.stdin)['id'])")
multica issue assign $C --to "<一个真实成员 agent 名>"
```

- [ ] **Step 2：轮询 runs 直到终态，记录如何判定「跑完」**

```bash
multica issue runs $C --output json        # 记录 run 的 status 字段与终态值
# 取 task-id 后：
multica issue run-messages <task-id> --output json | tail
```
findings.md 记录：assign 是否自动触发 run、`runs` json 里**终态怎么表示**（success/failed/done…）——Phase 2 的轮询解析靠它。

- [ ] **Step 3：判定 go/no-go #1**

检查该 agent 是否真的产出了 git 分支 + PR。**结论二选一写进 findings.md：**
- ✅ 能产 PR → Phase 2 的 worker→PR→gate 按 spec §3.5 实现。
- ❌ 不能产 PR → **停止，回报用户**：multica agent 无法产 gateable 产物，§3.5 闸不成立，需重新设计（如 worker 仍由 leader spawn worktree agent，仅用 multica 做跟踪）。

### Task 0.3：验证「leader-as-agent 能跑引擎 + multica CLI」（go/no-go #2）

- [ ] **Step 1：在 leader agent 的运行环境里探测 python/git/multica 可用性**

给 leader agent 派一个一次性探测任务（issue 描述）："运行 `python3 --version`、`git --version`、`multica squad member list <你的 squad-id> --output json`，回报输出。"

- [ ] **Step 2：判定 go/no-go #2，写 findings.md**
- ✅ 三者可用 → leader 直接在自身环境跑 `run_dag.py`。
- ❌ 不可用 → 回报用户：引擎需在 leader 环境外运行（如用户本机），调整部署假设。

> **Phase 0 完成判据：** findings.md 含三项结论 + 真实 JSON 样本；go/no-go #1/#2 均为 ✅ 才继续 Phase 2；纯逻辑 Phase 1 不受 gate 影响，可并行先做。

---

## Phase 1 — 引擎纯逻辑核（TDD，与 pilot 无关）

> 全部用 `FakeMulticaClient` 单测，不碰真 CLI。模块边界：`manifest.py`(数据模型) / `lint.py`(校验) / `graph.py`(建图+frontier+失败隔离) / `client.py`(IO 接口 + Fake) / `compile.py`(manifest→metadata) / `engine.py`(driver 循环)。

**File Structure:**
- Create `SKILL_DIR/scripts/manifest.py` — manifest 加载 + schema 校验
- Create `SKILL_DIR/scripts/lint.py` — manifest×roster 校验规则
- Create `SKILL_DIR/scripts/graph.py` — 归一化 issue → classify/frontier/失败隔离/完成判定
- Create `SKILL_DIR/scripts/client.py` — `MulticaClient` 抽象接口 + `FakeMulticaClient`（测试用）
- Create `SKILL_DIR/scripts/compile.py` — manifest 单向编译进 issue metadata
- Create `SKILL_DIR/scripts/engine.py` — 固定 driver：frontier→dispatch→gate→status，含并发封顶/失败隔离/续跑
- Create `SKILL_DIR/scripts/tests/test_*.py` — 各模块单测

### Task 1：manifest 数据模型与加载（manifest.py）

**Files:**
- Create: `SKILL_DIR/scripts/manifest.py`
- Test: `SKILL_DIR/scripts/tests/test_manifest.py`

- [ ] **Step 1：写失败测试**

```python
# tests/test_manifest.py
import pytest
from manifest import load_manifest, Manifest, Node

def test_load_minimal_manifest(tmp_path):
    p = tmp_path / "m.yaml"
    p.write_text(
        "meta:\n  squad: dev team\n  integration_branch: feature/v1.0.0\n"
        "nodes:\n"
        "  - id: M0\n    worker: agent-be\n    blocked_by: []\n"
        "  - id: M1\n    worker: agent-fe\n    blocked_by: [M0]\n    reviewer: agent-rev\n    risk: high\n"
    )
    m = load_manifest(str(p))
    assert isinstance(m, Manifest)
    assert m.meta["integration_branch"] == "feature/v1.0.0"
    assert m.nodes["M1"].blocked_by == ["M0"]
    assert m.nodes["M1"].reviewer == "agent-rev"
    assert m.nodes["M0"].reviewer is None   # 可选旋钮缺省 None
    assert m.nodes["M1"].risk == "high"

def test_missing_required_worker_raises(tmp_path):
    p = tmp_path / "m.yaml"
    p.write_text("meta: {squad: x}\nnodes:\n  - id: M0\n    blocked_by: []\n")
    with pytest.raises(ValueError, match="worker"):
        load_manifest(str(p))
```

- [ ] **Step 2：运行确认失败**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_manifest.py -v`
Expected: FAIL（`No module named 'manifest'`）

- [ ] **Step 3：最小实现**

```python
# manifest.py
from dataclasses import dataclass, field
import yaml

@dataclass
class Node:
    id: str
    worker: str
    blocked_by: list = field(default_factory=list)
    reviewer: str | None = None
    risk: str | None = None
    gate: dict | None = None

@dataclass
class Manifest:
    meta: dict
    nodes: dict  # id -> Node

def load_manifest(path: str) -> Manifest:
    with open(path) as f:
        raw = yaml.safe_load(f)
    nodes = {}
    for n in raw.get("nodes", []):
        if "id" not in n:
            raise ValueError("node missing 'id'")
        if not n.get("worker"):
            raise ValueError(f"node {n['id']} missing required 'worker'")
        nodes[n["id"]] = Node(
            id=n["id"], worker=n["worker"],
            blocked_by=list(n.get("blocked_by", [])),
            reviewer=n.get("reviewer"), risk=n.get("risk"), gate=n.get("gate"),
        )
    return Manifest(meta=raw.get("meta", {}), nodes=nodes)
```

- [ ] **Step 4：运行确认通过**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_manifest.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5：提交**

```bash
git add SKILL_DIR/scripts/manifest.py SKILL_DIR/scripts/tests/test_manifest.py
git commit -m "feat(multica-orch): manifest 数据模型与加载"
```

### Task 2：lint 校验规则（lint.py）

**Files:**
- Create: `SKILL_DIR/scripts/lint.py`
- Test: `SKILL_DIR/scripts/tests/test_lint.py`

- [ ] **Step 1：写失败测试**

```python
# tests/test_lint.py
from manifest import Manifest, Node
from lint import lint

POOL = {"agent-be", "agent-fe", "agent-rev"}

def mk(nodes):  # nodes: list[Node]
    return Manifest(meta={}, nodes={n.id: n for n in nodes})

def test_clean_manifest_no_errors():
    m = mk([Node("M0", "agent-be"), Node("M1", "agent-fe", ["M0"], reviewer="agent-rev")])
    assert lint(m, POOL) == []

def test_worker_not_in_pool():
    m = mk([Node("M0", "ghost")])
    errs = lint(m, POOL)
    assert any("ghost" in e and "pool" in e for e in errs)

def test_blocked_by_unknown_node():
    m = mk([Node("M0", "agent-be", ["NOPE"])])
    assert any("NOPE" in e for e in lint(m, POOL))

def test_cycle_detected():
    m = mk([Node("A", "agent-be", ["B"]), Node("B", "agent-fe", ["A"])])
    assert any("cycle" in e.lower() for e in lint(m, POOL))

def test_reviewer_equals_worker():
    m = mk([Node("M0", "agent-be", reviewer="agent-be")])
    assert any("reviewer" in e and "worker" in e for e in lint(m, POOL))
```

- [ ] **Step 2：运行确认失败**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_lint.py -v`
Expected: FAIL（`No module named 'lint'`）

- [ ] **Step 3：最小实现**

```python
# lint.py
from manifest import Manifest

def _has_cycle(nodes):
    WHITE, GREY, BLACK = 0, 1, 2
    color = {k: WHITE for k in nodes}
    def dfs(u):
        color[u] = GREY
        for v in nodes[u].blocked_by:
            if v not in nodes:        # 未知引用，交给别的规则报
                continue
            if color[v] == GREY:
                return True
            if color[v] == WHITE and dfs(v):
                return True
        color[u] = BLACK
        return False
    return any(color[k] == WHITE and dfs(k) for k in nodes)

def lint(m: Manifest, pool: set) -> list:
    errs = []
    for n in m.nodes.values():
        if n.worker not in pool:
            errs.append(f"node {n.id}: worker '{n.worker}' not in squad pool")
        for b in n.blocked_by:
            if b not in m.nodes:
                errs.append(f"node {n.id}: blocked_by references unknown node '{b}'")
        if n.reviewer is not None and n.reviewer == n.worker:
            errs.append(f"node {n.id}: reviewer must differ from worker")
    if _has_cycle(m.nodes):
        errs.append("manifest DAG has a cycle")
    return errs
```

- [ ] **Step 4：运行确认通过**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_lint.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5：提交**

```bash
git add SKILL_DIR/scripts/lint.py SKILL_DIR/scripts/tests/test_lint.py
git commit -m "feat(multica-orch): manifest lint（无环/worker∈池/依赖存在/reviewer≠worker）"
```

### Task 3：归一化 issue 与 frontier（graph.py）

**Files:**
- Create: `SKILL_DIR/scripts/graph.py`
- Test: `SKILL_DIR/scripts/tests/test_graph.py`

> 归一化 issue dict 字段：`key, id, status, blocked_by(list[key]), worker, reviewer, review_verdict`。`status` 取多创原生：todo/in_progress/in_review/done/blocked/cancelled。

- [ ] **Step 1：写失败测试**

```python
# tests/test_graph.py
from graph import frontier, is_done, all_terminal

def iss(key, status, blocked_by=None):
    return {"key": key, "id": key, "status": status,
            "blocked_by": blocked_by or [], "worker": "w", "reviewer": None,
            "review_verdict": None}

def test_frontier_root_ready_when_todo():
    issues = {"M0": iss("M0", "todo")}
    assert frontier(issues) == ["M0"]

def test_frontier_blocked_until_dep_done():
    issues = {"M0": iss("M0", "in_progress"), "M1": iss("M1", "todo", ["M0"])}
    assert frontier(issues) == []           # M0 未 done，M1 不 ready；M0 在飞不算 frontier
    issues["M0"]["status"] = "done"
    assert frontier(issues) == ["M1"]       # M0 done → M1 解锁

def test_inflight_and_done_excluded_from_frontier():
    issues = {"A": iss("A", "in_review"), "B": iss("B", "done"), "C": iss("C", "todo")}
    assert frontier(issues) == ["C"]

def test_all_terminal_done_or_cancelled():
    assert all_terminal({"A": iss("A", "done"), "B": iss("B", "cancelled")})
    assert not all_terminal({"A": iss("A", "done"), "B": iss("B", "todo")})
```

- [ ] **Step 2：运行确认失败**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_graph.py -v`
Expected: FAIL（`No module named 'graph'`）

- [ ] **Step 3：最小实现**

```python
# graph.py
DONE = "done"
TERMINAL = {"done", "cancelled"}
INFLIGHT = {"in_progress", "in_review"}

def is_done(issue) -> bool:
    return issue["status"] == DONE

def frontier(issues: dict) -> list:
    """status==todo 且所有 blocked_by 节点已 done 的节点 key（连续 frontier，不分层）。"""
    out = []
    for key, it in issues.items():
        if it["status"] != "todo":
            continue
        if all(issues.get(b, {}).get("status") == DONE for b in it["blocked_by"]):
            out.append(key)
    return out

def all_terminal(issues: dict) -> bool:
    return all(it["status"] in TERMINAL for it in issues.values())
```

- [ ] **Step 4：运行确认通过**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_graph.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5：提交**

```bash
git add SKILL_DIR/scripts/graph.py SKILL_DIR/scripts/tests/test_graph.py
git commit -m "feat(multica-orch): graph frontier/完成判定（连续 frontier）"
```

### Task 4：失败分支隔离（graph.py 扩展）

**Files:**
- Modify: `SKILL_DIR/scripts/graph.py`
- Test: `SKILL_DIR/scripts/tests/test_graph.py`（追加）

> 失败用 `status=="blocked"` 表示（多创原生失败态）。`downstream_of(failed_keys)` 返回所有传递依赖了失败节点的 key——这些不应再被 dispatch；健康分支照常。

- [ ] **Step 1：追加失败测试**

```python
# tests/test_graph.py 追加
from graph import downstream_of, frontier

def test_downstream_of_blocks_dependents_only():
    issues = {
        "A": iss("A", "blocked"),            # 失败
        "B": iss("B", "todo", ["A"]),        # 依赖失败 → 下游
        "C": iss("C", "todo", ["B"]),        # 传递下游
        "D": iss("D", "done"),               # 无关健康
        "E": iss("E", "todo", ["D"]),        # 健康分支，应仍 ready
    }
    assert downstream_of(issues, {"A"}) == {"B", "C"}
    # 健康分支 E 不受影响，仍在 frontier
    assert "E" in frontier(issues)
```

- [ ] **Step 2：运行确认失败**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_graph.py::test_downstream_of_blocks_dependents_only -v`
Expected: FAIL（`cannot import name 'downstream_of'`）

- [ ] **Step 3：实现**

```python
# graph.py 追加
def downstream_of(issues: dict, failed_keys: set) -> set:
    """所有（传递）依赖了 failed_keys 的节点 key。"""
    rev = {k: set() for k in issues}          # blocker -> dependents
    for k, it in issues.items():
        for b in it["blocked_by"]:
            if b in rev:
                rev[b].add(k)
    out, stack = set(), list(failed_keys)
    while stack:
        cur = stack.pop()
        for dep in rev.get(cur, ()):
            if dep not in out:
                out.add(dep)
                stack.append(dep)
    return out
```

- [ ] **Step 4：运行确认通过**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_graph.py -v`
Expected: PASS（全部）

- [ ] **Step 5：提交**

```bash
git add SKILL_DIR/scripts/graph.py SKILL_DIR/scripts/tests/test_graph.py
git commit -m "feat(multica-orch): 失败分支隔离 downstream_of"
```

### Task 5：MulticaClient 接口 + FakeMulticaClient（client.py）

**Files:**
- Create: `SKILL_DIR/scripts/client.py`
- Test: `SKILL_DIR/scripts/tests/test_client_fake.py`

> `MulticaClient` 是 IO 边界抽象（Phase 2 实现真 CLI 版）。`FakeMulticaClient` 用内存 dict 模拟，供 compile/engine 单测。方法面：`list_issues()`, `set_metadata(key, k, v)`, `set_status(key, status)`, `assign(key, agent)`, `runs(key)`, `squad_members(squad_id)`。

- [ ] **Step 1：写测试**

```python
# tests/test_client_fake.py
from client import FakeMulticaClient

def test_fake_assign_then_status_and_list():
    c = FakeMulticaClient(issues={
        "M0": {"key": "M0", "id": "M0", "status": "todo", "blocked_by": [],
               "worker": None, "reviewer": None, "review_verdict": None},
    })
    c.assign("M0", "agent-be")
    c.set_status("M0", "in_progress")
    got = c.list_issues()
    assert got["M0"]["status"] == "in_progress"
    assert got["M0"]["assignee"] == "agent-be"

def test_fake_set_metadata_roundtrip():
    c = FakeMulticaClient(issues={"M1": {"key": "M1", "id": "M1", "status": "todo",
        "blocked_by": [], "worker": None, "reviewer": None, "review_verdict": None}})
    c.set_metadata("M1", "blocked_by", ["M0"])
    c.set_metadata("M1", "worker", "agent-fe")
    got = c.list_issues()["M1"]
    assert got["blocked_by"] == ["M0"] and got["worker"] == "agent-fe"
```

- [ ] **Step 2：运行确认失败**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_client_fake.py -v`
Expected: FAIL（`No module named 'client'`）

- [ ] **Step 3：实现接口 + Fake**

```python
# client.py
from abc import ABC, abstractmethod

class MulticaClient(ABC):
    @abstractmethod
    def list_issues(self) -> dict: ...
    @abstractmethod
    def set_metadata(self, key, k, v): ...
    @abstractmethod
    def set_status(self, key, status): ...
    @abstractmethod
    def assign(self, key, agent): ...
    @abstractmethod
    def runs(self, key) -> list: ...
    @abstractmethod
    def squad_members(self, squad_id) -> list: ...

class FakeMulticaClient(MulticaClient):
    def __init__(self, issues=None, members=None, run_log=None):
        self.issues = issues or {}
        self.members = members or []
        self.run_log = run_log or {}     # key -> list of run dicts
    def list_issues(self):
        return {k: dict(v) for k, v in self.issues.items()}
    def set_metadata(self, key, k, v):
        self.issues[key][k] = v
    def set_status(self, key, status):
        self.issues[key]["status"] = status
    def assign(self, key, agent):
        self.issues[key]["assignee"] = agent
    def runs(self, key):
        return list(self.run_log.get(key, []))
    def squad_members(self, squad_id):
        return list(self.members)
```

- [ ] **Step 4：运行确认通过**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_client_fake.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5：提交**

```bash
git add SKILL_DIR/scripts/client.py SKILL_DIR/scripts/tests/test_client_fake.py
git commit -m "feat(multica-orch): MulticaClient 接口 + FakeMulticaClient"
```

### Task 6：manifest 单向编译进 metadata（compile.py）

**Files:**
- Create: `SKILL_DIR/scripts/compile.py`
- Test: `SKILL_DIR/scripts/tests/test_compile.py`

- [ ] **Step 1：写测试**

```python
# tests/test_compile.py
from manifest import Manifest, Node
from client import FakeMulticaClient
from compile import compile_manifest

def test_compile_writes_blocked_by_and_worker():
    m = Manifest(meta={}, nodes={
        "M0": Node("M0", "agent-be"),
        "M1": Node("M1", "agent-fe", ["M0"], reviewer="agent-rev", risk="high"),
    })
    c = FakeMulticaClient(issues={
        "M0": {"key": "M0", "id": "M0", "status": "todo", "blocked_by": [], "worker": None,
               "reviewer": None, "review_verdict": None},
        "M1": {"key": "M1", "id": "M1", "status": "todo", "blocked_by": [], "worker": None,
               "reviewer": None, "review_verdict": None},
    })
    compile_manifest(m, c)
    got = c.list_issues()
    assert got["M1"]["blocked_by"] == ["M0"]
    assert got["M1"]["worker"] == "agent-fe"
    assert got["M1"]["reviewer"] == "agent-rev"
    assert got["M1"]["risk"] == "high"
    assert got["M0"]["blocked_by"] == []     # 空依赖也显式写
```

- [ ] **Step 2：运行确认失败**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_compile.py -v`
Expected: FAIL（`No module named 'compile'`）

- [ ] **Step 3：实现**

```python
# compile.py
from manifest import Manifest
from client import MulticaClient

def compile_manifest(m: Manifest, client: MulticaClient):
    """单向编译：manifest 节点的 blocked_by/worker/旋钮 → issue metadata。"""
    for node in m.nodes.values():
        client.set_metadata(node.id, "blocked_by", list(node.blocked_by))
        client.set_metadata(node.id, "worker", node.worker)
        if node.reviewer is not None:
            client.set_metadata(node.id, "reviewer", node.reviewer)
        if node.risk is not None:
            client.set_metadata(node.id, "risk", node.risk)
```

- [ ] **Step 4：运行确认通过**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_compile.py -v`
Expected: PASS（1 passed）

- [ ] **Step 5：提交**

```bash
git add SKILL_DIR/scripts/compile.py SKILL_DIR/scripts/tests/test_compile.py
git commit -m "feat(multica-orch): manifest 单向编译进 metadata"
```

### Task 7：引擎 driver 循环（engine.py，over FakeMulticaClient）

**Files:**
- Create: `SKILL_DIR/scripts/engine.py`
- Test: `SKILL_DIR/scripts/tests/test_engine.py`

> driver 用**可注入的回调**模拟「派 worker」「派 reviewer」的结果，使纯逻辑可测：`run_dag(client, dispatch_worker, run_gate, max_parallel)`。`dispatch_worker(key)->("ok"|"fail")`；`run_gate(key)->("approve"|"changes")`。引擎据此推进 status 并解锁下游；失败按分支隔离；终止时返回 `{"done":[...], "failed":[...]}`。真派发/真轮询在 Phase 2 注入真实现。

- [ ] **Step 1：写测试（成功推进 + 失败隔离 + 续跑跳过 done）**

```python
# tests/test_engine.py
from client import FakeMulticaClient
from engine import run_dag

def base(issues):
    return FakeMulticaClient(issues={k: dict(key=k, id=k, status=s, blocked_by=b,
        worker="w", reviewer=None, review_verdict=None) for k, (s, b) in issues.items()})

def test_linear_dag_runs_to_done():
    c = base({"M0": ("todo", []), "M1": ("todo", ["M0"])})
    res = run_dag(c, dispatch_worker=lambda k: "ok", run_gate=lambda k: "approve", max_parallel=4)
    assert set(res["done"]) == {"M0", "M1"} and res["failed"] == []
    assert c.list_issues()["M1"]["status"] == "done"

def test_worker_failure_isolates_downstream():
    c = base({"A": ("todo", []), "B": ("todo", ["A"]), "D": ("todo", [])})
    res = run_dag(c, dispatch_worker=lambda k: "fail" if k == "A" else "ok",
                  run_gate=lambda k: "approve", max_parallel=4)
    assert "A" in res["failed"]
    assert "B" not in res["done"]            # 下游被隔离，未跑
    assert "D" in res["done"]                # 健康分支照常完成

def test_gate_changes_then_no_progress_reported_failed():
    c = base({"M0": ("todo", [])})
    res = run_dag(c, dispatch_worker=lambda k: "ok",
                  run_gate=lambda k: "changes", max_parallel=1)
    assert "M0" in res["failed"] and res["done"] == []   # 评审打回且本轮未过 → 计入失败集交 orchestrator

def test_resume_skips_already_done():
    c = base({"M0": ("done", []), "M1": ("todo", ["M0"])})
    calls = []
    run_dag(c, dispatch_worker=lambda k: calls.append(k) or "ok",
            run_gate=lambda k: "approve", max_parallel=4)
    assert calls == ["M1"]                   # M0 已 done，不重派
```

- [ ] **Step 2：运行确认失败**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_engine.py -v`
Expected: FAIL（`No module named 'engine'`）

- [ ] **Step 3：实现 driver（单线程顺序版，max_parallel 预留并发位）**

```python
# engine.py
from graph import frontier, all_terminal, downstream_of

def run_dag(client, dispatch_worker, run_gate, max_parallel=4):
    """固定 driver：连续 frontier → 派 worker → gate → status；失败按分支隔离。
    返回 {"done":[...], "failed":[...]}。max_parallel 为并发上限（此实现顺序处理一批，
    真并发在 Phase 2 用线程池替换循环体，语义不变）。"""
    failed = set()
    while True:
        issues = client.list_issues()
        # 隔离：已失败节点的下游不再 ready
        blocked = downstream_of(issues, failed)
        ready = [k for k in frontier(issues) if k not in blocked and k not in failed]
        if not ready:
            break
        for key in ready[:max_parallel] if max_parallel else ready:
            client.assign(key, issues[key].get("worker") or "w")
            client.set_status(key, "in_progress")
            if dispatch_worker(key) != "ok":
                client.set_status(key, "blocked")
                failed.add(key)
                continue
            client.set_status(key, "in_review")
            if run_gate(key) == "approve":
                client.set_status(key, "done")
            else:
                client.set_status(key, "blocked")
                failed.add(key)
    issues = client.list_issues()
    done = [k for k, it in issues.items() if it["status"] == "done"]
    return {"done": done, "failed": sorted(failed)}
```

- [ ] **Step 4：运行确认通过**

Run: `cd SKILL_DIR/scripts && python -m pytest tests/test_engine.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5：全量回归 + 提交**

Run: `cd SKILL_DIR/scripts && python -m pytest -v`
Expected: PASS（全部 Phase 1 测试）

```bash
git add SKILL_DIR/scripts/engine.py SKILL_DIR/scripts/tests/test_engine.py
git commit -m "feat(multica-orch): 引擎 driver（连续 frontier + 失败隔离 + 续跑）"
```

---

## Phase 2 — 真 multica CLI client + 入口（pilot 后细化）

> 依赖 Phase 0 findings（`runs` 终态字段、metadata 读写格式、squad/leader 解析）。任务清单与接口已定，**具体解析实现待 findings 回流**：

- **Task 2.1** 实现 `CliMulticaClient(MulticaClient)`：每方法 `subprocess` 调 `multica …`，`list_issues()` 映射 `issue list --output json` → 归一化（status、metadata.blocked_by/worker/reviewer/review_verdict）。按 findings 的真实字段写映射 + 解析。
- **Task 2.2** 实现 leader 自感知：`resolve_squad()`（`squad list` 找 leader_id==self）+ `squad_members()` 取非 leader agent 池。
- **Task 2.3** 实现真 `dispatch_worker(key)`：`assign(key, worker)` → 轮询 `runs(key)` 到终态（终态值取自 findings）→ 检查 PR/CI（git）。
- **Task 2.4** 实现真 `run_gate(key)`：`set_status in_review` → `assign(key, reviewer≠worker)` → 轮询 reviewer run → 读 `metadata.review_verdict`。
- **Task 2.5** `run_dag.py` CLI 入口：装配 manifest→lint→compile→`run_dag`(真 client/回调)；输出 digest；`squad activity` 记录决策。并发版 driver（线程池替换 Task 7 循环体，语义不变）。
- **Task 2.6** 对真 workspace 跑一次端到端小 DAG（2-3 节点）冒烟。

## Phase 3 — executor skill + SKILL.md + 打包（pilot 后）

- **Task 3.1** 写 `executor-skill/`（status 自感知：in_progress→worker 半[测试先行/只 import 契约/PR base=集成分支]；in_review→verifier 半[只读共享态/diff×口径×红线/验收↔test 映射/**强制独立复跑测试**/写结构化 review_verdict]）。
- **Task 3.2** 写 `SKILL.md`（术：阶段 0–E 操作流程，引方法论为道，引本 spec 为口径；含 squad-native roster、manifest 约定、装 skill 命令）。
- **Task 3.3** 写 `manifest.example.yaml` + `pilot/findings.md` 模板。
- **Task 3.4** 移植/装备清单：`agent skills add` 给 leader/members 装 skill 的一次性步骤文档化。

---

## Self-Review（已执行）

- **Spec coverage：** §3.1 squad roster→Task 2.2；§3.2 metadata-DAG→Task 3/6；§3.3 manifest+lint→Task 1/2；§3.4 引擎语义（连续 frontier/失败隔离/续跑）→Task 3/4/7；§3.5 worker/reviewer 闸→Task 2.3/2.4/3.1；§4 流程→SKILL.md(Task 3.2)；§10 pilot→Phase 0。覆盖完整。
- **Placeholder scan：** Phase 1 各步均含完整可运行代码与断言；Phase 0 含真实命令；Phase 2/3 为「pilot 后细化」的有意分阶段（接口已定、非占位），原因是其实现依赖 Phase 0 findings，提前写死即投机——符合 spec §10 的 gate 设计。
- **Type consistency：** `Manifest/Node` 字段、`FakeMulticaClient` 方法面（list_issues/set_metadata/set_status/assign/runs/squad_members）、归一化 issue 字段（key/id/status/blocked_by/worker/reviewer/review_verdict）、`run_dag(client, dispatch_worker, run_gate, max_parallel)` 签名跨 Task 1/5/6/7 一致。
