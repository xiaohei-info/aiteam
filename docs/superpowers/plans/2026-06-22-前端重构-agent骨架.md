# 前端重构 · Phase 2 Agent 骨架（#127）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans。Steps 用 `- [ ]`。

**Goal:** 在 `shared/ui` 建可复用的 **AppShell**（黑金玻璃侧栏壳）与 **Workspace**（对话+时间线双栏壳）；Agent 端接入 app-kit（QueryProvider/ErrorBoundary）、把壳换成 AppShell、登录页重写为黑金玻璃；建立 `config/` 作本端差异配置位。

**Architecture:** AppShell/Workspace 为纯表现组件，router-agnostic（经 `renderLink` 注入链接组件），消费 design-system 玻璃质感。Agent 复用现有 DI（`lib/app-context` 的 AppProvider）与 `page-shell` 逻辑（`buildShellViewModel`），仅替换壳的渲染与 providers 组合。功能页（chat/group/workspace 内部）内容不变（各自在 #128/#129/#130 重写），仅运行在新壳内。

**Tech Stack:** React 18 · Tailwind v4 · react-router-dom 6 · `@aiteam/shared/{ui,app-kit,page-shell}` · Vitest + Testing Library。

---

## 范围与非范围

**做：** AppShell + Workspace（shared/ui，TDD）；Agent `app/providers.tsx`（ErrorBoundary+QueryProvider+AppProvider+Router）；`config/nav.ts`（迁自 lib/shell-config）；壳改用 AppShell（黑金）；LoginPage 黑金重写（保留 i18n key 与字段，测试不破）；全局应用 app.css 黑金基样。

**不做：** chat/group/workspace 三页内部重写（#128/#129/#130）；Manager/Operation（#131+）；`lib/` 内 DI 模块（api-client/app-context/auth-session/token-store/i18n）的改名搬迁——它们已清晰，保持原位以零 churn，命名收敛随各功能页重写时再议。

**硬约束：** 零后端/接口/端口/端点改动；保留 i18n key 与 `data-testid`、可访问角色；既有测试全绿；玻璃降级；桌面优先 + 平板（<768px）侧栏可收起。

---

## 文件结构

```
web/shared/src/ui/
  app-shell.tsx / app-shell.test.tsx       # Create: 黑金玻璃壳（侧栏+主区，响应式收起）
  workspace.tsx / workspace.test.tsx       # Create: 双栏（主区+可选右面板）
  index.ts                                 # Modify: 导出 AppShell/Workspace 及类型
web/agent/src/
  app/providers.tsx                        # Create: 全局 provider 组合
  app/App.tsx                              # Modify: 用 providers
  config/nav.ts                            # Create: 迁自 lib/shell-config.ts
  components/PageShell.tsx                 # Modify: 改用 shared AppShell 渲染（黑金）
  pages/LoginPage.tsx                      # Modify: 黑金玻璃重写（保留 i18n key/字段）
  lib/shell-config.ts                      # Delete: 内容迁到 config/nav.ts
```

---

## Task 1: AppShell（shared/ui，黑金玻璃壳 + 响应式）

**Files:** Create `web/shared/src/ui/app-shell.tsx` + `app-shell.test.tsx`

- [ ] **Step 1: 写失败测试**

`web/shared/src/ui/app-shell.test.tsx`：
```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { AppShell } from "./app-shell.js";

const nav = [
  { id: "a", label: "工作台", path: "/workspace", active: true },
  { id: "b", label: "私聊", path: "/chat", active: false },
];

function renderShell() {
  return render(
    <AppShell
      brand={<span>AI Team</span>}
      nav={nav}
      renderLink={(item, className) => (
        <a href={item.path} className={className} data-testid={`nav-${item.id}`}>
          {item.label}
        </a>
      )}
    >
      <div>主区内容</div>
    </AppShell>,
  );
}

describe("AppShell", () => {
  it("渲染品牌、导航项（经 renderLink）与主区", () => {
    renderShell();
    expect(screen.getByText("AI Team")).toBeInTheDocument();
    expect(screen.getByTestId("nav-a")).toHaveTextContent("工作台");
    expect(screen.getByText("主区内容")).toBeInTheDocument();
  });

  it("激活项带激活样式类", () => {
    renderShell();
    expect(screen.getByTestId("nav-a").className).toContain("text-gold");
    expect(screen.getByTestId("nav-b").className).not.toContain("text-gold");
  });

  it("窄屏菜单按钮切换侧栏展开（aria-expanded 翻转）", async () => {
    renderShell();
    const toggle = screen.getByRole("button", { name: /菜单|menu/i });
    expect(toggle).toHaveAttribute("aria-expanded", "false");
    await userEvent.click(toggle);
    expect(toggle).toHaveAttribute("aria-expanded", "true");
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run（`web/shared`）：`pnpm test app-shell`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 app-shell.tsx**

`web/shared/src/ui/app-shell.tsx`：
```tsx
import { useState, type ReactNode } from "react";
import { cn } from "./cn.js";

export interface AppShellNavItem {
  id: string;
  label: string;
  path: string;
  active: boolean;
}

export interface AppShellProps {
  brand: ReactNode;
  nav: AppShellNavItem[];
  /** 由消费端注入链接组件（router-agnostic）；className 含激活态样式。 */
  renderLink: (item: AppShellNavItem, className: string) => ReactNode;
  footer?: ReactNode;
  children: ReactNode;
}

const itemBase =
  "block rounded-md px-md py-sm text-sm transition no-underline";
const itemIdle = "text-text-secondary hover:text-text-primary hover:bg-surface-raised";
const itemActive = "text-gold bg-surface-raised";

/**
 * 黑金玻璃应用壳：玻璃侧栏（品牌+导航+footer）+ 主区。
 * 桌面侧栏常驻；<768px 经菜单按钮收起/展开（overlay）。表现-only，可测。
 */
export function AppShell({ brand, nav, renderLink, footer, children }: AppShellProps): ReactNode {
  const [open, setOpen] = useState(false);
  return (
    <div className="flex h-screen bg-bg-canvas text-text-primary">
      <aside
        className={cn(
          "glass z-sticky flex w-60 flex-col gap-xs p-md",
          "max-md:fixed max-md:inset-y-0 max-md:left-0 max-md:transition-transform",
          open ? "max-md:translate-x-0" : "max-md:-translate-x-full",
        )}
      >
        <div className="mb-md px-xs text-lg font-bold text-gold">{brand}</div>
        <nav className="flex flex-col gap-xs">
          {nav.map((item) => (
            <span key={item.id}>
              {renderLink(item, cn(itemBase, item.active ? itemActive : itemIdle))}
            </span>
          ))}
        </nav>
        {footer ? <div className="mt-auto pt-md text-sm text-text-muted">{footer}</div> : null}
      </aside>
      <div className="flex min-w-0 flex-1 flex-col">
        <button
          type="button"
          aria-label="菜单"
          aria-expanded={open}
          onClick={() => setOpen((v) => !v)}
          className="glass m-sm h-9 w-9 shrink-0 rounded-md text-text-secondary md:hidden"
        >
          ☰
        </button>
        <main className="min-h-0 flex-1 overflow-auto p-lg">{children}</main>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: 运行确认通过**

Run（`web/shared`）：`pnpm test app-shell`
Expected: PASS（3 用例）。

- [ ] **Step 5: Commit**
```bash
git -C /home/ubuntu/code/aiteam add web/shared/src/ui/app-shell.tsx web/shared/src/ui/app-shell.test.tsx
git -C /home/ubuntu/code/aiteam commit -m "feat(ui): AppShell 黑金玻璃应用壳（响应式收起）"
```

---

## Task 2: Workspace（shared/ui，双栏壳）

**Files:** Create `web/shared/src/ui/workspace.tsx` + `workspace.test.tsx`

- [ ] **Step 1: 写失败测试**

`web/shared/src/ui/workspace.test.tsx`：
```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { Workspace } from "./workspace.js";

describe("Workspace", () => {
  it("渲染主区；有 panel 时渲染右面板与标题", () => {
    render(
      <Workspace
        panelTitle={<span>执行时间线</span>}
        panel={<div>步骤</div>}
      >
        <div>对话</div>
      </Workspace>,
    );
    expect(screen.getByText("对话")).toBeInTheDocument();
    expect(screen.getByText("执行时间线")).toBeInTheDocument();
    expect(screen.getByText("步骤")).toBeInTheDocument();
  });

  it("无 panel 时不渲染右面板区域", () => {
    render(<Workspace><div>仅对话</div></Workspace>);
    expect(screen.getByText("仅对话")).toBeInTheDocument();
    expect(screen.queryByTestId("ws-panel")).toBeNull();
  });
});
```

- [ ] **Step 2: 运行确认失败** — `pnpm test workspace`，Expected FAIL。

- [ ] **Step 3: 实现 workspace.tsx**

`web/shared/src/ui/workspace.tsx`：
```tsx
import type { ReactNode } from "react";
import { GlassPanel } from "./glass-panel.js";

export interface WorkspaceProps {
  /** 主区（对话/主内容）。 */
  children: ReactNode;
  /** 右侧面板内容（如执行时间线）；缺省则不渲染右栏。 */
  panel?: ReactNode;
  panelTitle?: ReactNode;
}

/** 双栏工作台：主区（flex-1）+ 可选右面板（玻璃，平板以下隐藏）。表现-only。 */
export function Workspace({ children, panel, panelTitle }: WorkspaceProps): ReactNode {
  return (
    <div className="flex h-full min-h-0 gap-md">
      <div className="flex min-w-0 flex-1 flex-col">{children}</div>
      {panel != null ? (
        <GlassPanel
          data-testid="ws-panel"
          className="hidden w-64 shrink-0 flex-col gap-sm rounded-window p-md lg:flex"
        >
          {panelTitle != null ? (
            <div className="text-xs font-bold tracking-wider text-text-muted">{panelTitle}</div>
          ) : null}
          <div className="min-h-0 flex-1 overflow-auto">{panel}</div>
        </GlassPanel>
      ) : null}
    </div>
  );
}
```

- [ ] **Step 4: 运行确认通过** — `pnpm test workspace`，Expected PASS（2 用例）。

- [ ] **Step 5: Commit**
```bash
git -C /home/ubuntu/code/aiteam add web/shared/src/ui/workspace.tsx web/shared/src/ui/workspace.test.tsx
git -C /home/ubuntu/code/aiteam commit -m "feat(ui): Workspace 对话+时间线双栏壳"
```

---

## Task 3: ui barrel 导出 + shared 构建

**Files:** Modify `web/shared/src/ui/index.ts`

- [ ] **Step 1: 增补导出**

`web/shared/src/ui/index.ts` 追加：
```ts
export { AppShell, type AppShellProps, type AppShellNavItem } from "./app-shell.js";
export { Workspace, type WorkspaceProps } from "./workspace.js";
```

- [ ] **Step 2: 构建 + 测试 + 子路径验证**

Run（`web/shared`）：
```bash
pnpm build && pnpm test && node -e "import('@aiteam/shared/ui').then(m=>console.log(Object.keys(m).sort().join(',')))"
```
Expected: 构建成功；shared 测试全绿；打印含 `AppShell,Button,GlassPanel,Workspace,cn`。

- [ ] **Step 3: Commit**
```bash
git -C /home/ubuntu/code/aiteam add web/shared/src/ui/index.ts
git -C /home/ubuntu/code/aiteam commit -m "feat(ui): 导出 AppShell 与 Workspace"
```

---

## Task 4: Agent config/ 配置位（迁 shell-config）

**Files:** Create `web/agent/src/config/nav.ts`；Delete `web/agent/src/lib/shell-config.ts`；Modify 引用处

- [ ] **Step 1: 建 config/nav.ts**

`web/agent/src/config/nav.ts`（内容同原 shell-config，仅迁位）：
```ts
/** 用户端 PageShell 导航配置（08 §12.2 / page-shell）。本端差异配置位。 */
import type { PageShellConfig } from "@aiteam/shared/page-shell";

export const agentShellConfig: PageShellConfig = {
  tier: "agent",
  titleKey: "agent.title",
  nav: [
    { id: "workspace", labelKey: "agent.nav.workspace", path: "/workspace", icon: "grid" },
    { id: "private-chat", labelKey: "agent.nav.private_chat", path: "/chat", icon: "message" },
    { id: "group-chat", labelKey: "agent.nav.group_chat", path: "/group", icon: "users" },
  ],
};
```

- [ ] **Step 2: 改引用 + 删旧文件**

Run（`web/agent`）：`grep -rn "lib/shell-config" src` 找到引用（PageShell.tsx）。把其 import 由 `../lib/shell-config` 改为 `../config/nav`。然后 `git rm src/lib/shell-config.ts`（或删除文件）。

- [ ] **Step 3: 类型检查**
Run（`web/agent`）：`pnpm typecheck`，Expected: 无错。

- [ ] **Step 4: Commit**
```bash
git -C /home/ubuntu/code/aiteam add web/agent/src/config/nav.ts web/agent/src/components/PageShell.tsx
git -C /home/ubuntu/code/aiteam rm web/agent/src/lib/shell-config.ts
git -C /home/ubuntu/code/aiteam commit -m "refactor(web/agent): 导航配置迁入 config/nav（骨架统一）"
```

---

## Task 5: Agent app/providers（接入 app-kit）

**Files:** Create `web/agent/src/app/providers.tsx`；Modify `web/agent/src/app/App.tsx`

- [ ] **Step 1: 建 providers.tsx**

`web/agent/src/app/providers.tsx`：
```tsx
import type { ReactNode } from "react";
import { BrowserRouter } from "react-router-dom";
import { ErrorBoundary, QueryProvider } from "@aiteam/shared/app-kit";

import { AppProvider } from "../lib/app-context";

/** 全局 provider 组合：错误边界 → 服务端状态 → 会话/DI → 路由。 */
export function AppProviders({ children }: { children: ReactNode }): ReactNode {
  return (
    <ErrorBoundary fallback={<div className="p-lg text-danger">应用出错，请刷新重试。</div>}>
      <QueryProvider>
        <BrowserRouter>
          <AppProvider>{children}</AppProvider>
        </BrowserRouter>
      </QueryProvider>
    </ErrorBoundary>
  );
}
```

- [ ] **Step 2: 改 App.tsx 用 providers**

`web/agent/src/app/App.tsx` 替换为：
```tsx
/** 应用根。装配全局 providers + 路由。 */
import { AppProviders } from "./providers";
import { AppRoutes } from "./routes";

export function App() {
  return (
    <AppProviders>
      <AppRoutes />
    </AppProviders>
  );
}
```

- [ ] **Step 3: 类型检查 + 测试**
Run（`web/agent`）：`pnpm typecheck && pnpm test` —— Expected：全绿（LoginPage/RequireAuth/features 测试自带 MemoryRouter+AppProvider，不受 App 根变更影响）。

- [ ] **Step 4: Commit**
```bash
git -C /home/ubuntu/code/aiteam add web/agent/src/app/providers.tsx web/agent/src/app/App.tsx
git -C /home/ubuntu/code/aiteam commit -m "feat(web/agent): app/providers 接入 QueryProvider 与 ErrorBoundary"
```

---

## Task 6: Agent 壳改用 AppShell（黑金）

**Files:** Modify `web/agent/src/components/PageShell.tsx`

- [ ] **Step 1: 重写 PageShell 用 shared AppShell**

`web/agent/src/components/PageShell.tsx`：
```tsx
/** 用户端路由壳：基于 page-shell 视图模型，用 shared AppShell（黑金）渲染。 */
import { Link, useLocation } from "react-router-dom";
import { buildShellViewModel } from "@aiteam/shared/page-shell";
import { AppShell, type AppShellNavItem } from "@aiteam/shared/ui";

import { agentShellConfig } from "../config/nav";
import { useApp } from "../lib/app-context";

export function PageShell({ children }: { children: React.ReactNode }) {
  const { session, i18n } = useApp();
  const { pathname } = useLocation();
  const model = buildShellViewModel(agentShellConfig, session, pathname);
  const nav: AppShellNavItem[] = model.nav.map((item) => ({
    id: item.id,
    label: i18n.t(item.labelKey),
    path: item.path,
    active: item.id === model.activeItemId,
  }));

  return (
    <AppShell
      brand={i18n.t(model.titleKey)}
      nav={nav}
      renderLink={(item, className) => (
        <Link to={item.path} className={className}>
          {item.label}
        </Link>
      )}
    >
      {children}
    </AppShell>
  );
}
```

> 注：改用 `useLocation()` 取当前路径（替代原 `window.location.pathname`），激活态在路由变化时正确刷新。

- [ ] **Step 2: 类型检查 + 测试**
Run（`web/agent`）：`pnpm typecheck && pnpm test` —— Expected：全绿。group.test 第 133 行注释提到 PageShell 侧栏“群聊”导航项——确认其 `.chat-list__header` 断言属群聊页内部（#129），本任务不影响；导航项文本仍存在。

- [ ] **Step 3: Commit**
```bash
git -C /home/ubuntu/code/aiteam add web/agent/src/components/PageShell.tsx
git -C /home/ubuntu/code/aiteam commit -m "feat(web/agent): 壳改用 shared AppShell（黑金玻璃）"
```

---

## Task 7: Agent 登录页黑金重写

**Files:** Modify `web/agent/src/pages/LoginPage.tsx`；Modify `web/agent/src/main.tsx`（app.css 作基样）

- [ ] **Step 1: 全局基样切到 app.css**

`web/agent/src/main.tsx`：把 `import "./styles/tokens.css";` 之外**保留**（features 仍用旧 BEM），确保 `import "./styles/app.css";` 在其后（已于 #138 加入）。无需改动则跳过。确认 `app.css` 含 `body{background}`基础：在 `app.css` 追加：
```css
body { background: var(--color-bg-canvas); color: var(--color-text-primary); }
```

- [ ] **Step 2: 重写 LoginPage（黑金玻璃，保留 i18n key 与字段标签）**

`web/agent/src/pages/LoginPage.tsx`：
```tsx
/** 登录页（03 §9.4C 本地登录）。黑金玻璃质感；保留 i18n key 与字段，行为不变。 */
import { useState, type FormEvent } from "react";
import { useNavigate } from "react-router-dom";
import { GlassPanel, Button } from "@aiteam/shared/ui";

import { useApiError, useApp } from "../lib/app-context";

const fieldCls =
  "rounded-md border border-glass-border bg-surface px-md py-sm text-sm text-text-primary " +
  "outline-none focus:ring-2 focus:ring-gold";

export function LoginPage() {
  const { client, i18n, applyLogin } = useApp();
  const toMessage = useApiError();
  const navigate = useNavigate();

  const [account, setAccount] = useState("");
  const [password, setPassword] = useState("");
  const [tenantHint, setTenantHint] = useState("");
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  async function handleSubmit(event: FormEvent): Promise<void> {
    event.preventDefault();
    setSubmitting(true);
    setError(null);
    try {
      const result = await client.login({
        account,
        password,
        tenant_hint: tenantHint.trim() || null,
      });
      if (!result) {
        setError(i18n.t("error.unknown"));
        return;
      }
      applyLogin(result.token, result.claims);
      navigate("/workspace");
    } catch (err) {
      setError(toMessage(err));
    } finally {
      setSubmitting(false);
    }
  }

  return (
    <div className="flex h-screen items-center justify-center bg-bg-canvas">
      <GlassPanel className="flex w-[360px] flex-col gap-md rounded-window p-xl">
        <form className="flex flex-col gap-md" onSubmit={handleSubmit}>
          <h1 className="m-0 text-xl font-bold text-text-primary">{i18n.t("agent.login.title")}</h1>
          <label className="flex flex-col gap-xs">
            <span className="text-xs text-text-secondary">{i18n.t("agent.login.account")}</span>
            <input
              className={fieldCls}
              type="text"
              value={account}
              autoComplete="username"
              onChange={(e) => setAccount(e.target.value)}
              required
            />
          </label>
          <label className="flex flex-col gap-xs">
            <span className="text-xs text-text-secondary">{i18n.t("agent.login.password")}</span>
            <input
              className={fieldCls}
              type="password"
              value={password}
              autoComplete="current-password"
              onChange={(e) => setPassword(e.target.value)}
              required
            />
          </label>
          <label className="flex flex-col gap-xs">
            <span className="text-xs text-text-secondary">{i18n.t("agent.login.tenant_hint")}</span>
            <input
              className={fieldCls}
              type="text"
              value={tenantHint}
              onChange={(e) => setTenantHint(e.target.value)}
            />
          </label>
          {error ? <div className="text-xs text-danger">{error}</div> : null}
          <Button type="submit" disabled={submitting} className="mt-sm">
            {submitting ? i18n.t("agent.login.loading") : i18n.t("agent.login.submit")}
          </Button>
        </form>
      </GlassPanel>
    </div>
  );
}
```

- [ ] **Step 3: 类型检查 + 测试**
Run（`web/agent`）：`pnpm typecheck && pnpm test` —— Expected：全绿（LoginPage.test 用 role=heading「登录」、getByLabelText 字段、role=button「登录」，i18n key 不变故通过）。

- [ ] **Step 4: Commit**
```bash
git -C /home/ubuntu/code/aiteam add web/agent/src/pages/LoginPage.tsx web/agent/src/styles/app.css
git -C /home/ubuntu/code/aiteam commit -m "feat(web/agent): 登录页黑金玻璃重写"
```

---

## Task 8: 全量校验 + 收尾

- [ ] **Step 1: web-ci 等价（web 根）**
```bash
cd web && pnpm install --frozen-lockfile && pnpm --filter @aiteam/shared run build && pnpm -r typecheck && pnpm -r test && pnpm -r build
```
Expected: 全绿。

- [ ] **Step 2: 零后端改动确认**
```bash
git -C /home/ubuntu/code/aiteam diff --name-only origin/feature/v1.0.0...HEAD | grep -E "^(server/|app/|\.hermes/)" && echo VIOLATION || echo "OK: 仅前端改动"
```
Expected: `OK: 仅前端改动`。

- [ ] **Step 3: 视觉自检（构建产物含黑金壳类）**
```bash
cd web/agent && CSS=$(ls dist/assets/index-*.css|head -1); grep -o "\.glass" "$CSS"|head -1; grep -o "text-gold" "$CSS"|head -1
```
Expected: 打印 `.glass` 与 `text-gold`（壳与登录的黑金类已编译进产物）。

---

## 风险与回退

| 风险 | 应对 |
|---|---|
| AppShell 响应式 overlay 在 jsdom 无法验证视觉 | 仅测行为（aria-expanded 翻转）；视觉靠产物 grep + 后续人工 |
| 换壳后 features 旧 BEM 与黑金壳风格不一致 | 预期，中途态；#128/#129/#130 各页重写时消除 |
| useLocation 需在 Router 内 | PageShell 仅在 RequireAuth 包裹的受保护路由内（已在 BrowserRouter 下），满足 |

## Self-Review
- Spec 覆盖：AppShell/Workspace（§4 壳）→Task1/2；app-kit 接入（§5.3/5.4）→Task5；config 分离（§5.2）→Task4；登录黑金（§2）→Task7；玻璃降级复用 #138 `.glass`。
- 占位符：无。
- 类型一致：`AppShellProps/AppShellNavItem/WorkspaceProps`、`AppProviders`、`agentShellConfig` 命名定义与引用一致。
