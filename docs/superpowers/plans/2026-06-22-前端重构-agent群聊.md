# 前端重构 · Phase 2 Agent 群聊页（#129）Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans。

**Goal:** Agent 群聊（group）三组件（GroupPage/GroupExpertRoster/MentionComposer）黑金玻璃重写，复用已黑金化的 chat 组件（ConversationList/TimelineView）+ shared GlassPanel/Button，行为与 useGroupApi/parseMentions 零改，保留全部 role/aria/文本/testid 契约。

**Architecture:** 纯表现重写。GroupPage 复用 chat 的 ConversationList/TimelineView；roster/triggered/mention 专属件换黑金 Tailwind。`useGroupApi`（groupDispatch）与 `parseMentions`（导出+受测）逻辑不动。@提及经 window CustomEvent 解耦机制保留。

## 范围/非范围
- 做：GroupPage、GroupExpertRoster、MentionComposer 黑金重写。
- 不做：workspace（#130）、useGroupApi 逻辑、后端。

## 硬约束
零后端改动；D6（展示态 roster/triggered/signal 不写持久化、只消费 BusinessTimelineEvent）；保留 role=log、aria-label「本会话专家」「群聊消息内容」、按钮 aria-label「@提及 {handle}」、文本「群聊A/B」「@专家A/B」「将触发：@…」「本轮 @提及触发：@…」「发送/发送中…」「选择一个群聊会话开始多专家协作」；既有 group.test 全绿；**不得用无效类**（border-glass-border / 不存在的 --z-* 等）——产物 grep 复核。

## 文件
```
web/agent/src/features/group/GroupPage.tsx          # Modify: 黑金布局
web/agent/src/features/group/GroupExpertRoster.tsx  # Modify: 黑金 roster
web/agent/src/features/group/MentionComposer.tsx    # Modify: 黑金 + Button（保留 parseMentions 导出）
```

## Task 1: GroupExpertRoster 黑金
- [ ] 重写：保留 aria-label「本会话专家」、header「专家（n）」、handle 按钮 aria-label「@提及 {handle}」与文本「@{handle}」、model 标、空态；BEM→Tailwind（金色 handle chip）。
- [ ] `pnpm test group` 绿。Commit `feat(web/agent): 群聊 roster 黑金重写`。

## Task 2: MentionComposer 黑金（Button）
- [ ] 重写：保留 `parseMentions` 导出与逻辑、textarea aria-label「群聊消息内容」、placeholder、`将触发：` 提示、错误、提交逻辑；发送按钮换 shared Button（可见名「发送/发送中…」、disabled 条件）；CustomEvent 监听保留；BEM→Tailwind。
- [ ] `pnpm test group` 绿。Commit `feat(web/agent): 群聊输入器黑金重写`。

## Task 3: GroupPage 黑金布局
- [ ] 重写：复用 ConversationList(headerLabel「群聊」)/TimelineView;roster-bar + triggered 提示(aria-live)黑金;空态文案保留;GlassPanel 包聊天区。
- [ ] `pnpm test`(agent 全量) 绿。Commit `feat(web/agent): 群聊页黑金布局`。

## Task 4: 全量校验
- [ ] `cd web && pnpm --filter @aiteam/shared run build && pnpm -r typecheck && pnpm -r test && pnpm -r build` 全绿。
- [ ] 零后端 grep；无 border-glass-border 残留 grep；产物黑金类 grep。

## Self-Review
契约逐条列入；useGroupApi/parseMentions 不动；无占位符。
