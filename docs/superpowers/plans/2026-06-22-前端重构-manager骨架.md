# 前端重构 · Phase 3 Manager 骨架（#131）Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans。

**Goal:** Manager 端接入 Tailwind v4 + app-kit（QueryProvider/ErrorBoundary）；壳换成 shared AppShell（黑金，复用 #127）；登录页 + Dashboard 黑金玻璃重写。保留全部 i18n key/testid/role/行为；features 页（members/experts/grants/governance）暂留旧 BEM（各自 #132-#134 重写）。

**Architecture:** 复用 shared AppShell（renderLink 注入 react-router Link，Outlet 作 children）+ page-shell 逻辑（buildShellViewModel）+ 现有 useSession/useI18n。Tailwind v4 接法同 agent（@tailwindcss/vite + app.css @import shared token，@source 扫 shared/src）。global.css 暂留（features BEM 依赖），app.css 叠加黑金基样。登录为既有脚手架（不真调后端）——仅 reskin 保留行为。

## 范围/非范围
- 做：Tailwind v4 接入；app-kit providers；AppShell 黑金；LoginPage + Dashboard 黑金。
- 不做：members/experts/grants/governance 页（#132-#134）；真实登录联调（既有脚手架行为不变）；后端。

## 硬约束
零后端改动；保留 i18n key、testid、role；登录文本「AI Team 企业端」「成员账号」「登录密码」「登录」、行为（空提交提示、已登录跳转）不变；LoginPage.test 的 `.login-page__form` class 断言改 testid；既有 manager 测试全绿；无效类排查；玻璃降级。

## 文件
```
web/manager/package.json            # Modify: + tailwindcss + @tailwindcss/vite
web/manager/vite.config.ts          # Modify: 挂 tailwindcss()
web/manager/src/styles/app.css      # Create: @import tailwind + shared token + body 基样
web/manager/src/main.tsx            # Modify: import app.css + 包 ErrorBoundary/QueryProvider
web/manager/src/shell/AppShell.tsx  # Modify: 用 shared AppShell（黑金，useLocation）
web/manager/src/pages/LoginPage.tsx # Modify: 黑金玻璃 + form testid
web/manager/src/pages/LoginPage.test.tsx  # Modify: class 断言改 testid
web/manager/src/pages/DashboardPlaceholder.tsx  # Modify: 黑金
```

## Task 1: Manager 接入 Tailwind v4 + app.css
- [ ] 安装：`cd web/manager && pnpm add -D tailwindcss@^4.0.0 @tailwindcss/vite@^4.0.0`。
- [ ] vite.config：import tailwindcss，plugins 加 `tailwindcss()`。
- [ ] 建 `src/styles/app.css`：`@import "tailwindcss";` + `@import "@aiteam/shared/design-system/tokens.css";` + `@source "../../../shared/src";` + body 黑金基样。
- [ ] main.tsx：在 global.css 后 `import "./styles/app.css";`。
- [ ] `pnpm typecheck` 绿。Commit `feat(web/manager): 接入 Tailwind v4 + 黑金 token`。

## Task 2: app-kit providers
- [ ] main.tsx：用 `ErrorBoundary`(fallback)+`QueryProvider` 包裹 `BrowserRouter>AppProviders>App`（ErrorBoundary 最外）。
- [ ] `pnpm test` 绿。Commit `feat(web/manager): 接入 QueryProvider 与 ErrorBoundary`。

## Task 3: 壳换 shared AppShell（黑金）
- [ ] 重写 shell/AppShell.tsx：requiresLogin → `<Outlet/>`；否则 `<AppShell brand nav renderLink={Link}><Outlet/></AppShell>`，nav 由 vm 映射（active=id===vm.activeItemId），用 `useLocation()` 取 path。
- [ ] `pnpm test` 绿。Commit `feat(web/manager): 壳改用 shared AppShell（黑金）`。

## Task 4: 登录页黑金 + 测试 testid
- [ ] 重写 LoginPage：GlassPanel + Button + 黑金输入；form 加 `data-testid="login-form"`；保留 i18n key/文本/行为（空提交提示、已登录 Navigate）。
- [ ] LoginPage.test：`container.querySelector(".login-page__form")` → `screen.queryByTestId("login-form")`。
- [ ] `pnpm test` 绿。Commit `feat(web/manager): 登录页黑金重写`。

## Task 5: Dashboard 黑金
- [ ] 重写 DashboardPlaceholder：GlassPanel 包裹，保留 i18n key（manager.nav.dashboard / manager.placeholder）。
- [ ] `pnpm test` 绿。Commit `feat(web/manager): Dashboard 黑金重写`。

## Task 6: 全量校验
- [ ] `cd web && pnpm --filter @aiteam/shared run build && pnpm -r typecheck && pnpm -r test && pnpm -r build` 全绿。
- [ ] 零后端 grep；无效类 grep；产物黑金类 grep（manager dist 应含 .glass/text-gold）。

## Self-Review
契约逐条列入；复用 shared AppShell；不做 features 页与真实登录；无占位符。
