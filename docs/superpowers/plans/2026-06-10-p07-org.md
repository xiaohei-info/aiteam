# P07 Org Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 P07 组织架构补齐合同与交互回归证据，确保组织树读取和归属 PATCH 可稳定验收。

**Architecture:** 保持现有 `/api/team/org/tree` 与 `/api/team/org/assignments/{id}` 不扩路径，只用测试先行方式补 layer2/layer4 验证；若发现真实缺口，再做最小实现。

**Tech Stack:** Python pytest, Node VM-based frontend tests, Team Panel org APIs

---

### Task 1: 补 P07 layer2 合同测试

**Files:**
- Create: `app/tests/aiteam/layer2_team_panel/test_org_contracts.py`

- [ ] **Step 1: 写失败测试**

覆盖：
- `GET /api/team/org/tree` 空企业 / 有部门有成员 shape
- `PATCH /api/team/org/assignments/{id}` 成功更新
- `PATCH` 非法字段 / 空 patch / 不存在部门

- [ ] **Step 2: 跑测试确认当前状态**

Run: `pytest app/tests/aiteam/layer2_team_panel/test_org_contracts.py -q`

- [ ] **Step 3: 若失败则做最小实现**

只修暴露的 org API 缺口。

- [ ] **Step 4: 重跑测试**

Run: `pytest app/tests/aiteam/layer2_team_panel/test_org_contracts.py -q`

### Task 2: 补 P07 layer4 页面回归

**Files:**
- Modify: `app/tests/aiteam/layer4_frontend_bff/test_org_page.py`

- [ ] **Step 1: 写失败测试**

覆盖：
- 403 时页面提示
- PATCH 成功后反馈文案
- PATCH 失败时错误反馈

- [ ] **Step 2: 跑测试确认当前状态**

Run: `pytest app/tests/aiteam/layer4_frontend_bff/test_org_page.py -q`

- [ ] **Step 3: 若失败则做最小实现**

只修页面反馈或 API 消费缺口。

- [ ] **Step 4: 全量回归**

Run: `pytest app/tests/aiteam/layer2_team_panel/test_org_contracts.py app/tests/aiteam/layer4_frontend_bff/test_org_page.py -q`
