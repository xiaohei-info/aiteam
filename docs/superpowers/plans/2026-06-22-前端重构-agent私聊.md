# 前端重构 · Phase 2 Agent 私聊页（#128）Implementation Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans。Steps 用 `- [ ]`。

**Goal:** 把 Agent 私聊（chat）四个表现组件（ChatPage/ConversationList/TimelineView/MessageComposer）重写为黑金玻璃质感（复用 shared GlassPanel/Button + Tailwind token），行为与 API hook 不变，保留全部 role/aria-label/文本/testid 契约。

**Architecture:** 纯表现层重写——`useChatApi.ts`（数据访问）与 `TimelineStore` 消费逻辑（D6 只消费 BusinessTimelineEvent）零改动。组件 DOM 结构/role/aria 保持，仅替换 BEM class 为 Tailwind 黑金类、引入 GlassPanel/Button。`ConversationList` 被群聊页复用——其 header class 断言（group.test 的 `.chat-list__header`）改为 testid。

**Tech Stack:** React 18 · Tailwind v4 · `@aiteam/shared/ui` · Vitest（既有 chat.test 为回归网）。

## 范围 / 非范围
- 做：chat 四组件黑金重写；加 `data-testid` 强化契约；group.test 的 class 断言改 testid。
- 不做：群聊页本体（#129）、工作台（#130）、useChatApi 逻辑、后端。

## 硬约束
零后端/接口/端口改动；D6（只消费 BusinessTimelineEvent、展示态不写主状态）不变；保留 role=list/log、aria-label「会话列表」「对话时间线」「消息内容」、按钮「发送」、文案「加载更多」「加载更早」「暂无事件」「选择一个会话开始对话」；既有 chat.test + group.test 全绿。

## 文件结构
```
web/agent/src/features/chat/
  ChatPage.tsx           # Modify: 黑金布局（GlassPanel 双区）
  ConversationList.tsx   # Modify: 黑金 + header testid
  TimelineView.tsx       # Modify: 黑金事件流
  MessageComposer.tsx    # Modify: 黑金 + Button
web/agent/src/features/group/group.test.tsx  # Modify: .chat-list__header → testid
```

---

## Task 1: ConversationList 黑金重写（+ header testid）

**Files:** Modify `web/agent/src/features/chat/ConversationList.tsx`

- [ ] **Step 1: 重写表现（保留 role/aria/文本/逻辑，加 header testid）**
保持组件 props、state、loadFirst/loadMore 逻辑、`role="list"` aria-label「会话列表」、item `<button aria-pressed>`、title/state 文本、「加载更多/加载中…」不变；把 `chat-list*` BEM 换为 Tailwind 黑金类；根容器用 `GlassPanel`；header 加 `data-testid="conv-list-header"`。
- [ ] **Step 2: 测试** Run（`web/agent`）：`pnpm test chat` Expected: 全绿（行为未变）。
- [ ] **Step 3: Commit** `feat(web/agent): 私聊会话列表黑金重写`

## Task 2: TimelineView 黑金重写

**Files:** Modify `web/agent/src/features/chat/TimelineView.tsx`
- [ ] **Step 1:** 保留 `role="log"` aria-live/aria-label「对话时间线」、store 逻辑、loadOlder、payload 渲染、「加载更早/暂无事件」；BEM→Tailwind 黑金（事件卡 surface-raised、type 标 gold）。
- [ ] **Step 2: 测试** `pnpm test chat` Expected 全绿（getByText "msg-10"、role=log 不变）。
- [ ] **Step 3: Commit** `feat(web/agent): 私聊时间线黑金重写`

## Task 3: MessageComposer 黑金重写（Button）

**Files:** Modify `web/agent/src/features/chat/MessageComposer.tsx`
- [ ] **Step 1:** 保留 textarea aria-label「消息内容」、placeholder、提交逻辑；发送按钮换 shared `Button`（保留可见名「发送」/「发送中…」、disabled 条件）；BEM→Tailwind。
- [ ] **Step 2: 测试** `pnpm test chat` Expected 全绿（getByLabelText「消息内容」、button name「发送」不变）。
- [ ] **Step 3: Commit** `feat(web/agent): 私聊输入器黑金重写`

## Task 4: ChatPage 黑金布局 + group.test 改 testid

**Files:** Modify `web/agent/src/features/chat/ChatPage.tsx`、`web/agent/src/features/group/group.test.tsx`
- [ ] **Step 1:** ChatPage 用 Tailwind grid（会话列表 + GlassPanel 聊天区），空态文案「选择一个会话开始对话」不变。
- [ ] **Step 2:** group.test.tsx 第 ~134 行 `document.querySelectorAll(".chat-list__header")` 改为 `screen.getAllByTestId("conv-list-header")`（语义等价，断言其存在）。
- [ ] **Step 3: 测试** `pnpm test`（agent 全量）Expected: chat + group 全绿。
- [ ] **Step 4: Commit** `feat(web/agent): 私聊页黑金布局 + group 测试改 testid`

## Task 5: 全量校验 + 视觉自检
- [ ] **Step 1:** `cd web && pnpm --filter @aiteam/shared run build && pnpm -r typecheck && pnpm -r test && pnpm -r build` 全绿。
- [ ] **Step 2:** 零后端：`git diff --name-only origin/feature/v1.0.0...HEAD | grep -E "^(server/|app/|\.hermes/)" && echo VIOLATION || echo OK`。
- [ ] **Step 3:** 产物 grep 黑金类真编译（`.glass`/`text-gold`/`bg-surface-raised`）。

## Self-Review
- 契约保留：role/aria/文本/testid 全列入各 Task；group.test 的 class 断言转 testid（同源组件改动连带）。
- 占位符：无。逻辑零改（useChatApi/TimelineStore 不动）。
