# 前端重构 · Phase 2 Agent 工作台（#130）Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans。

**Goal:** Agent 工作台（WorkspacePage）黑金玻璃重写——近期会话 + Loop 状态/调度两段总览，用 GlassPanel 卡片 + Button，保留全部 i18n key / testid / Link / 行为；useWorkspaceApi 零改。

**Architecture:** 纯表现重写。工作台是本地总览仪表盘（非聊天双栏）——按最小必要黑金化，不强塞 Workspace 双栏（双栏壳已在 #127 建好，留待真正的 Loop/Run 执行详情视图落地时接入；当前后端无实时执行时间线数据源）。

## 范围/非范围
- 做：WorkspacePage 黑金重写。
- 不做：Workspace 双栏接入（无数据源，过度设计）、useWorkspaceApi、后端。

## 硬约束
零后端改动；D6/D13（展示态不持久化、本地内容不上传）；保留 testid `ws-conversation`/`ws-loop`、会话/Loop 标题文本、Link href `/chat`、按钮文案「启用/停用/立即触发」（i18n key 不变）、各 i18n 文案；既有 workspace.test 全绿；无效类排查（不得 border-glass-border 等）。

## 文件
`web/agent/src/features/workspace/WorkspacePage.tsx`  # Modify: 黑金重写

## Task 1: WorkspacePage 黑金重写
- [ ] 重写：标题/summary/错误/loading 用 Tailwind 黑金；会话与 Loop 两段用 GlassPanel 卡片；Loop 操作按钮换 shared Button（保留可见文案）；保留 testid/Link/i18n key/行为。
- [ ] `pnpm test workspace` 绿。Commit `feat(web/agent): 工作台黑金重写`。

## Task 2: 全量校验
- [ ] `cd web && pnpm --filter @aiteam/shared run build && pnpm -r typecheck && pnpm -r test && pnpm -r build` 全绿。
- [ ] 零后端 grep；无效类 grep；产物黑金类 grep。

## Self-Review
契约逐条列入；useWorkspaceApi 不动；不过度设计（不强塞双栏）；无占位符。
