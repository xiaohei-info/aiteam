# 前端重构 · Phase 1 地基（shared）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `web/shared` 从纯 TS 包升级为前端重构地基——黑金 design token 单一来源（生成 Tailwind v4 `@theme` CSS + 玻璃质感基样与降级）、Radix+Tailwind 自有组件库雏形（GlassPanel/Button）、app-kit 跨端通用层（TanStack Query / ErrorBoundary），并在一个消费端（agent）验证 Tailwind v4 集成贯通。

**Architecture:** `shared` 保留既有逻辑资产（contracts/api-client/timeline-client/role-state/page-shell/i18n）不动；`design-system` 重建为唯一 token 源，经构建脚本生成 `@theme` CSS（消灭旧"TS→3 份 CSS 手工镜像"）；新增 `ui`（组件库）与 `app-kit`（providers/query/error）。各消费端运行自己的 Tailwind v4（`@tailwindcss/vite`），`@import` shared 导出的 token CSS。依赖单向：消费端 → shared。

**Tech Stack:** TypeScript 5.5 · React 18.3 · Tailwind CSS v4（`@tailwindcss/vite`）· Radix UI · TanStack Query v5 · Vitest 2 + jsdom + Testing Library · tsc 构建（.tsx→dist）+ node 生成 token CSS。

---

## 范围与非范围

**本计划做（Phase 1）：** shared 构建/测试管线升级；黑金 token 体系 + `@theme` CSS 生成 + 玻璃基样与 `@supports` 降级；Tailwind v4 在 agent 端的集成确认；`GlassPanel`、`Button` 两个示范组件；`app-kit`（query client + QueryProvider + ErrorBoundary）；shared exports 收口；web-ci 全绿。

**本计划不做（后续计划）：** `AppShell`/`Workspace` 双栏壳与其余 ui 原语（随 Phase 2 Agent 端按需建，复用本计划组件模式）；各端页面表现层重写与统一骨架迁移（Phase 2/3/4，各自成篇）。

**硬约束：** 后端/接口/端口/端点/契约零改动；不动 contracts/api-client/timeline-client/role-state/page-shell/i18n 的逻辑；保留所有 `data-testid` 与可访问角色；守最小必要原则，不过度设计。

---

## 文件结构

```
web/shared/
  package.json                         # Modify: 加 deps/exports/build 脚本
  tsconfig.json                        # Modify: jsx + 含 .tsx
  vitest.config.ts                     # Modify: jsdom + plugin-react + setup
  test-setup.ts                        # Create: @testing-library/jest-dom 注入
  scripts/build-tokens-css.mjs         # Create: 由 tokens 生成 @theme + 玻璃基样 CSS
  src/
    design-system/
      tokens.ts                        # Modify: 重建为黑金 token 单一来源
      tokens.test.ts                   # Modify: 黑金 token 断言
      css.ts                           # Create: tokens → CSS 字符串（被脚本与测试共用）
      css.test.ts                      # Create: 生成 CSS 的断言（含 @supports 降级）
      index.ts                         # Modify: 导出 tokens + css 生成器
    ui/
      cn.ts                            # Create: className 合并工具（clsx+tailwind-merge）
      cn.test.ts                       # Create
      glass-panel.tsx                  # Create: GlassPanel（降级内建）
      glass-panel.test.tsx             # Create
      button.tsx                       # Create: Button（金属/幽灵/危险变体）
      button.test.tsx                  # Create
      index.ts                         # Create: ui barrel
    app-kit/
      query.ts                         # Create: createQueryClient 工厂
      query.test.ts                    # Create
      query-provider.tsx               # Create: QueryProvider
      error-boundary.tsx               # Create: ErrorBoundary
      error-boundary.test.tsx          # Create
      index.ts                         # Create: app-kit barrel
web/agent/
  package.json                         # Modify: 加 tailwindcss + @tailwindcss/vite
  vite.config.ts                       # Modify: 挂 @tailwindcss/vite 插件
  src/styles/app.css                   # Create: @import tailwind + shared token css
  src/styles/integration.test.tsx     # Create: 验证 GlassPanel 渲染 + 降级（集成确认）
```

---

## Task 1: shared 构建/测试管线升级（支持 React + jsdom）

**Files:**
- Modify: `web/shared/package.json`
- Modify: `web/shared/tsconfig.json`
- Modify: `web/shared/vitest.config.ts`
- Create: `web/shared/test-setup.ts`

- [ ] **Step 1: 安装 React/测试/组件依赖**

Run（在 `web/shared` 目录）：
```bash
cd web/shared
pnpm add react@^18.3.1 react-dom@^18.3.1 @tanstack/react-query@^5.59.0 clsx@^2.1.1 tailwind-merge@^2.5.0 @radix-ui/react-slot@^1.1.0
pnpm add -D @types/react@^18.3.11 @types/react-dom@^18.3.0 @testing-library/react@^16.0.1 @testing-library/jest-dom@^6.5.0 @vitejs/plugin-react@^4.3.2 jsdom@^25.0.1
```
Expected: 写入 `web/shared/package.json` 与根 `web/pnpm-lock.yaml`，无 `ERR_PNPM`。

- [ ] **Step 2: 改 tsconfig 支持 .tsx**

修改 `web/shared/tsconfig.json` 为：
```json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "jsx": "react-jsx",
    "types": ["vitest/globals", "@testing-library/jest-dom"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["dist", "node_modules", "src/**/*.test.ts", "src/**/*.test.tsx"]
}
```

- [ ] **Step 3: 改 vitest 用 jsdom + plugin-react**

修改 `web/shared/vitest.config.ts` 为：
```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: "jsdom",
    setupFiles: ["./test-setup.ts"],
    include: ["src/**/*.test.{ts,tsx}"],
    css: false,
  },
});
```

创建 `web/shared/test-setup.ts`：
```ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 4: 验证既有逻辑测试在 jsdom 下仍全绿**

Run（在 `web/shared`）：
```bash
pnpm test
```
Expected: PASS——既有 contracts/api-client/timeline-client/role-state/i18n/page-shell/design-system 测试在 jsdom 环境下全部通过（纯逻辑用例不依赖 node 专属 API）。

- [ ] **Step 5: Commit**

```bash
git add web/shared/package.json web/shared/tsconfig.json web/shared/vitest.config.ts web/shared/test-setup.ts web/pnpm-lock.yaml
git commit -m "build(web/shared): 升级构建/测试管线支持 React 组件与 jsdom"
```

---

## Task 2: 黑金 design token 单一来源

**Files:**
- Modify: `web/shared/src/design-system/tokens.ts`
- Modify: `web/shared/src/design-system/tokens.test.ts`
- Modify: `web/shared/src/design-system/index.ts`

- [ ] **Step 1: 写失败测试（黑金 token 断言）**

替换 `web/shared/src/design-system/tokens.test.ts` 内容：
```ts
import { describe, it, expect } from "vitest";
import { tokens } from "./tokens.js";

describe("黑金 design tokens", () => {
  it("画布为暖黑、品牌为金", () => {
    expect(tokens.color.bgCanvas).toBe("#0c0a07");
    expect(tokens.color.gold).toBe("#cda349");
  });

  it("含玻璃材质参数（模糊度/实色降级背景）", () => {
    expect(tokens.glass.blurPanel).toBe("30px");
    expect(tokens.glass.bgSolid).toBe("#16120d");
  });

  it("半径含窗体大圆角", () => {
    expect(tokens.radius.window).toBe("16px");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run（`web/shared`）：`pnpm test tokens.test`
Expected: FAIL —— `tokens.color.gold` / `tokens.glass` 未定义。

- [ ] **Step 3: 重建 tokens.ts**

替换 `web/shared/src/design-system/tokens.ts` 内容：
```ts
/**
 * 设计系统 · 黑金玻璃质感 design tokens（唯一来源）。
 * 框架无关的设计变量单一来源——经 design-system/css.ts 生成 Tailwind v4 @theme CSS，
 * 消灭旧"TS→多份 CSS 手工镜像"。各端只 @import 生成物，不复制取值。
 */

export const colorTokens = {
  bgCanvas: "#0c0a07",
  surface: "#16120d",
  surfaceRaised: "#1c1710",
  gold: "#cda349",
  goldBright: "#e7c873",
  goldDeep: "#b8862f",
  textPrimary: "#f3ecdc",
  textSecondary: "#b8ad97",
  textMuted: "#8a8068",
  success: "#4aa062",
  warning: "#e0a44a",
  danger: "#e5645a",
  info: "#6fb6d6",
} as const;

export const glassTokens = {
  blurPanel: "30px",
  blurSidebar: "18px",
  bgPanel: "rgba(22,18,13,0.72)",
  bgSidebar: "rgba(14,11,7,0.45)",
  bgSolid: "#16120d",
  border: "rgba(212,175,95,0.18)",
  highlight: "rgba(255,235,190,0.10)",
  shadow: "0 24px 70px rgba(0,0,0,0.55)",
} as const;

export const spaceTokens = {
  xs: "4px", sm: "8px", md: "16px", lg: "24px", xl: "32px", xxl: "48px",
} as const;

export const radiusTokens = {
  sm: "4px", md: "8px", lg: "12px", window: "16px", pill: "999px",
} as const;

export const fontSizeTokens = {
  xs: "12px", sm: "14px", md: "16px", lg: "20px", xl: "24px",
} as const;

export const zIndexTokens = {
  base: 0, dropdown: 1000, sticky: 1100, overlay: 1200, modal: 1300, toast: 1400,
} as const;

export const tokens = {
  color: colorTokens,
  glass: glassTokens,
  space: spaceTokens,
  radius: radiusTokens,
  fontSize: fontSizeTokens,
  zIndex: zIndexTokens,
} as const;

export type ColorToken = keyof typeof colorTokens;
export type GlassToken = keyof typeof glassTokens;
export type SpaceToken = keyof typeof spaceTokens;
export type RadiusToken = keyof typeof radiusTokens;
export type FontSizeToken = keyof typeof fontSizeTokens;

function kebab(s: string): string {
  return s.replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase();
}

export { kebab };
```

- [ ] **Step 4: 运行测试确认通过**

Run（`web/shared`）：`pnpm test tokens.test`
Expected: PASS。

- [ ] **Step 5: 更新 design-system barrel**

替换 `web/shared/src/design-system/index.ts` 内容：
```ts
export * from "./tokens.js";
export * from "./css.js";
```
（`css.js` 在 Task 3 创建；本步先写好导出，Task 3 后即可编译。）

- [ ] **Step 6: Commit**

```bash
git add web/shared/src/design-system/tokens.ts web/shared/src/design-system/tokens.test.ts web/shared/src/design-system/index.ts
git commit -m "feat(design-system): 黑金玻璃质感 token 单一来源"
```

---

## Task 3: token → Tailwind v4 @theme CSS 生成器（含降级）

**Files:**
- Create: `web/shared/src/design-system/css.ts`
- Create: `web/shared/src/design-system/css.test.ts`
- Create: `web/shared/scripts/build-tokens-css.mjs`
- Modify: `web/shared/package.json`（build 脚本 + exports）

- [ ] **Step 1: 写失败测试**

创建 `web/shared/src/design-system/css.test.ts`：
```ts
import { describe, it, expect } from "vitest";
import { buildTokensCss } from "./css.js";

describe("buildTokensCss", () => {
  const css = buildTokensCss();

  it("产出 Tailwind v4 @theme 块且含金色变量", () => {
    expect(css).toContain("@theme");
    expect(css).toContain("--color-gold: #cda349;");
    expect(css).toContain("--color-bg-canvas: #0c0a07;");
  });

  it("含玻璃基样 .glass 与 @supports 降级（不支持时退实色 #16120d）", () => {
    expect(css).toContain(".glass");
    expect(css).toContain("backdrop-filter");
    expect(css).toContain("@supports not (backdrop-filter: blur(1px))");
    expect(css).toContain("#16120d");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run（`web/shared`）：`pnpm test css.test`
Expected: FAIL —— `buildTokensCss` 未定义。

- [ ] **Step 3: 实现 css.ts**

创建 `web/shared/src/design-system/css.ts`：
```ts
/**
 * 由 tokens 生成 Tailwind v4 @theme CSS + 玻璃质感基样（含 @supports 降级）。
 * 唯一生成入口——消费端 @import 生成物，不手抄取值。
 */
import {
  colorTokens, glassTokens, spaceTokens, radiusTokens, fontSizeTokens, kebab,
} from "./tokens.js";

export function buildTokensCss(): string {
  const lines: string[] = [];
  lines.push("@theme {");
  for (const [k, v] of Object.entries(colorTokens)) lines.push(`  --color-${kebab(k)}: ${v};`);
  for (const [k, v] of Object.entries(spaceTokens)) lines.push(`  --spacing-${k}: ${v};`);
  for (const [k, v] of Object.entries(radiusTokens)) lines.push(`  --radius-${k}: ${v};`);
  for (const [k, v] of Object.entries(fontSizeTokens)) lines.push(`  --text-${k}: ${v};`);
  for (const [k, v] of Object.entries(glassTokens)) lines.push(`  --glass-${kebab(k)}: ${v};`);
  lines.push("}");
  lines.push("");
  // 玻璃基样：默认毛玻璃；不支持 backdrop-filter 时退化为实色暗面板（不丢可用性）。
  lines.push(".glass {");
  lines.push(`  background: ${glassTokens.bgPanel};`);
  lines.push(`  backdrop-filter: blur(${glassTokens.blurPanel}) saturate(150%);`);
  lines.push(`  -webkit-backdrop-filter: blur(${glassTokens.blurPanel}) saturate(150%);`);
  lines.push(`  border: 1px solid ${glassTokens.border};`);
  lines.push(`  box-shadow: ${glassTokens.shadow}, inset 0 1px 0 ${glassTokens.highlight};`);
  lines.push("}");
  lines.push("@supports not (backdrop-filter: blur(1px)) {");
  lines.push(`  .glass { background: ${glassTokens.bgSolid}; }`);
  lines.push("}");
  lines.push("");
  return lines.join("\n");
}
```

- [ ] **Step 4: 运行测试确认通过**

Run（`web/shared`）：`pnpm test css.test`
Expected: PASS。

- [ ] **Step 5: 创建构建脚本 + 接入 package.json**

创建 `web/shared/scripts/build-tokens-css.mjs`：
```js
// tsc 不产出 .css；本脚本在 tsc 后由编译产物生成 token CSS 到 dist。
import { writeFileSync } from "node:fs";
import { fileURLToPath } from "node:url";
import { dirname, resolve } from "node:path";
import { buildTokensCss } from "../dist/design-system/css.js";

const here = dirname(fileURLToPath(import.meta.url));
const out = resolve(here, "../dist/design-system/tokens.css");
writeFileSync(out, buildTokensCss(), "utf8");
console.log(`wrote ${out}`);
```

修改 `web/shared/package.json`：将 `"build"` 改为 `"tsc -p tsconfig.json && node scripts/build-tokens-css.mjs"`，并在 `exports` 增加：
```json
"./design-system/tokens.css": "./dist/design-system/tokens.css"
```

- [ ] **Step 6: 验证构建产出 CSS**

Run（`web/shared`）：
```bash
pnpm build && test -f dist/design-system/tokens.css && grep -q "@theme" dist/design-system/tokens.css && echo OK
```
Expected: 打印 `wrote .../tokens.css` 与 `OK`。

- [ ] **Step 7: Commit**

```bash
git add web/shared/src/design-system/css.ts web/shared/src/design-system/css.test.ts web/shared/scripts/build-tokens-css.mjs web/shared/package.json
git commit -m "feat(design-system): 生成 Tailwind v4 @theme CSS 与玻璃降级基样"
```

---

## Task 4: className 合并工具 cn

**Files:**
- Create: `web/shared/src/ui/cn.ts`
- Create: `web/shared/src/ui/cn.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `web/shared/src/ui/cn.test.ts`：
```ts
import { describe, it, expect } from "vitest";
import { cn } from "./cn.js";

describe("cn", () => {
  it("合并并去重冲突的 tailwind 类（后者胜）", () => {
    expect(cn("px-2 py-1", "px-4")).toBe("py-1 px-4");
  });
  it("忽略假值", () => {
    expect(cn("a", false, undefined, "b")).toBe("a b");
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run（`web/shared`）：`pnpm test cn.test`
Expected: FAIL —— `cn` 未定义。

- [ ] **Step 3: 实现 cn.ts**

创建 `web/shared/src/ui/cn.ts`：
```ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

/** 合并 className：clsx 处理条件，tailwind-merge 解决类冲突。 */
export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 4: 运行确认通过**

Run（`web/shared`）：`pnpm test cn.test`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web/shared/src/ui/cn.ts web/shared/src/ui/cn.test.ts
git commit -m "feat(ui): className 合并工具 cn"
```

---

## Task 5: GlassPanel 组件（签名组件，降级内建）

**Files:**
- Create: `web/shared/src/ui/glass-panel.tsx`
- Create: `web/shared/src/ui/glass-panel.test.tsx`

- [ ] **Step 1: 写失败测试**

创建 `web/shared/src/ui/glass-panel.test.tsx`：
```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { GlassPanel } from "./glass-panel.js";

describe("GlassPanel", () => {
  it("渲染子节点且带 glass 基类（降级由 CSS @supports 承担）", () => {
    render(<GlassPanel data-testid="p">内容</GlassPanel>);
    const el = screen.getByTestId("p");
    expect(el).toHaveTextContent("内容");
    expect(el.className).toContain("glass");
  });

  it("合并外部 className 且可透传属性", () => {
    render(<GlassPanel data-testid="p" className="p-md" role="region" />);
    const el = screen.getByTestId("p");
    expect(el.className).toContain("p-md");
    expect(el).toHaveAttribute("role", "region");
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run（`web/shared`）：`pnpm test glass-panel`
Expected: FAIL —— 模块不存在。

- [ ] **Step 3: 实现 glass-panel.tsx**

创建 `web/shared/src/ui/glass-panel.tsx`：
```tsx
import { forwardRef, type HTMLAttributes } from "react";
import { cn } from "./cn.js";

/**
 * 毛玻璃面板。质感与降级在 design-system 的 `.glass` 基样（@supports 自动退实色），
 * 本组件只负责挂类与透传属性——表现与逻辑分离、可测。
 */
export const GlassPanel = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(
  function GlassPanel({ className, ...rest }, ref) {
    return <div ref={ref} className={cn("glass", className)} {...rest} />;
  },
);
```

- [ ] **Step 4: 运行确认通过**

Run（`web/shared`）：`pnpm test glass-panel`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web/shared/src/ui/glass-panel.tsx web/shared/src/ui/glass-panel.test.tsx
git commit -m "feat(ui): GlassPanel 毛玻璃面板（降级内建）"
```

---

## Task 6: Button 组件（金属/幽灵/危险变体）

**Files:**
- Create: `web/shared/src/ui/button.tsx`
- Create: `web/shared/src/ui/button.test.tsx`

- [ ] **Step 1: 写失败测试**

创建 `web/shared/src/ui/button.test.tsx`：
```tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { Button } from "./button.js";

describe("Button", () => {
  it("默认 metal 变体可点击并触发 onClick", async () => {
    const onClick = vi.fn();
    render(<Button onClick={onClick}>发送</Button>);
    await userEvent.click(screen.getByRole("button", { name: "发送" }));
    expect(onClick).toHaveBeenCalledOnce();
  });

  it("disabled 时不触发 onClick", async () => {
    const onClick = vi.fn();
    render(<Button disabled onClick={onClick}>发送</Button>);
    await userEvent.click(screen.getByRole("button", { name: "发送" }));
    expect(onClick).not.toHaveBeenCalled();
  });

  it("danger 变体带对应类", () => {
    render(<Button variant="danger">删除</Button>);
    expect(screen.getByRole("button").className).toContain("bg-danger");
  });
});
```

- [ ] **Step 2: 安装 user-event 并运行确认失败**

Run（`web/shared`）：
```bash
pnpm add -D @testing-library/user-event@^14.5.2
pnpm test button.test
```
Expected: FAIL —— `Button` 未定义。

- [ ] **Step 3: 实现 button.tsx**

创建 `web/shared/src/ui/button.tsx`：
```tsx
import { forwardRef, type ButtonHTMLAttributes } from "react";
import { Slot } from "@radix-ui/react-slot";
import { cn } from "./cn.js";

type Variant = "metal" | "ghost" | "danger";
type Size = "sm" | "md";

const base =
  "inline-flex items-center justify-center font-semibold rounded-md transition " +
  "disabled:opacity-50 disabled:cursor-not-allowed focus-visible:outline-none " +
  "focus-visible:ring-2 focus-visible:ring-gold";

const variants: Record<Variant, string> = {
  metal:
    "text-bg-canvas bg-gradient-to-br from-gold-bright via-gold to-gold-deep " +
    "shadow-[inset_0_1px_1px_rgba(255,255,255,0.5),0_6px_16px_rgba(196,150,60,0.3)]",
  ghost: "text-text-secondary hover:text-text-primary hover:bg-surface-raised",
  danger: "text-bg-canvas bg-danger hover:brightness-110",
};

const sizes: Record<Size, string> = {
  sm: "h-8 px-3 text-sm",
  md: "h-10 px-5 text-sm",
};

export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: Variant;
  size?: Size;
  /** 渲染为子元素（如包裹链接），借 Radix Slot 合并属性。 */
  asChild?: boolean;
}

export const Button = forwardRef<HTMLButtonElement, ButtonProps>(function Button(
  { variant = "metal", size = "md", asChild = false, className, ...rest },
  ref,
) {
  const Comp = asChild ? Slot : "button";
  return <Comp ref={ref} className={cn(base, variants[variant], sizes[size], className)} {...rest} />;
});
```

- [ ] **Step 4: 运行确认通过**

Run（`web/shared`）：`pnpm test button.test`
Expected: PASS（3 用例）。

- [ ] **Step 5: 创建 ui barrel**

创建 `web/shared/src/ui/index.ts`：
```ts
export { cn } from "./cn.js";
export { GlassPanel } from "./glass-panel.js";
export { Button, type ButtonProps } from "./button.js";
```

- [ ] **Step 6: Commit**

```bash
git add web/shared/src/ui/button.tsx web/shared/src/ui/button.test.tsx web/shared/src/ui/index.ts web/shared/package.json web/pnpm-lock.yaml
git commit -m "feat(ui): Button 金属/幽灵/危险变体 + ui barrel"
```

---

## Task 7: app-kit —— TanStack Query 客户端工厂

**Files:**
- Create: `web/shared/src/app-kit/query.ts`
- Create: `web/shared/src/app-kit/query.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `web/shared/src/app-kit/query.test.ts`：
```ts
import { describe, it, expect } from "vitest";
import { createQueryClient } from "./query.js";

describe("createQueryClient", () => {
  it("默认不在窗口聚焦时重抓、查询重试 1 次（统一服务端状态策略）", () => {
    const qc = createQueryClient();
    const def = qc.getDefaultOptions();
    expect(def.queries?.refetchOnWindowFocus).toBe(false);
    expect(def.queries?.retry).toBe(1);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run（`web/shared`）：`pnpm test query.test`
Expected: FAIL —— `createQueryClient` 未定义。

- [ ] **Step 3: 实现 query.ts**

创建 `web/shared/src/app-kit/query.ts`：
```ts
import { QueryClient } from "@tanstack/react-query";

/** 跨端统一的 QueryClient 默认策略——消灭各端手写 useEffect+fetch 的样板与边界 if。 */
export function createQueryClient(): QueryClient {
  return new QueryClient({
    defaultOptions: {
      queries: {
        refetchOnWindowFocus: false,
        retry: 1,
        staleTime: 30_000,
      },
    },
  });
}
```

- [ ] **Step 4: 运行确认通过**

Run（`web/shared`）：`pnpm test query.test`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web/shared/src/app-kit/query.ts web/shared/src/app-kit/query.test.ts
git commit -m "feat(app-kit): 统一 QueryClient 工厂"
```

---

## Task 8: app-kit —— QueryProvider 与 ErrorBoundary

**Files:**
- Create: `web/shared/src/app-kit/query-provider.tsx`
- Create: `web/shared/src/app-kit/error-boundary.tsx`
- Create: `web/shared/src/app-kit/error-boundary.test.tsx`
- Create: `web/shared/src/app-kit/index.ts`

- [ ] **Step 1: 写失败测试（ErrorBoundary）**

创建 `web/shared/src/app-kit/error-boundary.test.tsx`：
```tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import { ErrorBoundary } from "./error-boundary.js";

function Boom(): never {
  throw new Error("炸了");
}

describe("ErrorBoundary", () => {
  it("子树抛错时渲染兜底 UI 而非崩整页", () => {
    const spy = vi.spyOn(console, "error").mockImplementation(() => {});
    render(
      <ErrorBoundary fallback={<div>出错了</div>}>
        <Boom />
      </ErrorBoundary>,
    );
    expect(screen.getByText("出错了")).toBeInTheDocument();
    spy.mockRestore();
  });

  it("正常时透传子节点", () => {
    render(
      <ErrorBoundary fallback={<div>出错了</div>}>
        <div>正常</div>
      </ErrorBoundary>,
    );
    expect(screen.getByText("正常")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run（`web/shared`）：`pnpm test error-boundary`
Expected: FAIL —— 模块不存在。

- [ ] **Step 3: 实现 error-boundary.tsx**

创建 `web/shared/src/app-kit/error-boundary.tsx`：
```tsx
import { Component, type ErrorInfo, type ReactNode } from "react";

interface Props {
  fallback: ReactNode;
  children: ReactNode;
  onError?: (error: Error, info: ErrorInfo) => void;
}
interface State {
  hasError: boolean;
}

/** React 错误边界——子树抛错时降级到 fallback，避免整页白屏。 */
export class ErrorBoundary extends Component<Props, State> {
  override state: State = { hasError: false };

  static getDerivedStateFromError(): State {
    return { hasError: true };
  }

  override componentDidCatch(error: Error, info: ErrorInfo): void {
    this.props.onError?.(error, info);
  }

  override render(): ReactNode {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
}
```

- [ ] **Step 4: 实现 query-provider.tsx**

创建 `web/shared/src/app-kit/query-provider.tsx`：
```tsx
import { useState, type ReactNode } from "react";
import { QueryClientProvider } from "@tanstack/react-query";
import { createQueryClient } from "./query.js";

/** 包裹应用以提供统一 QueryClient；client 经 useState 惰性单例，避免重渲染重建。 */
export function QueryProvider({ children }: { children: ReactNode }): ReactNode {
  const [client] = useState(createQueryClient);
  return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
}
```

- [ ] **Step 5: 创建 app-kit barrel**

创建 `web/shared/src/app-kit/index.ts`：
```ts
export { createQueryClient } from "./query.js";
export { QueryProvider } from "./query-provider.js";
export { ErrorBoundary } from "./error-boundary.js";
```

- [ ] **Step 6: 运行确认通过**

Run（`web/shared`）：`pnpm test error-boundary`
Expected: PASS（2 用例）。

- [ ] **Step 7: Commit**

```bash
git add web/shared/src/app-kit/
git commit -m "feat(app-kit): QueryProvider 与 ErrorBoundary"
```

---

## Task 9: shared exports 收口 + 构建/类型/测试全绿

**Files:**
- Modify: `web/shared/package.json`（exports 增加 ui / app-kit）

- [ ] **Step 1: 增加 exports 子路径**

修改 `web/shared/package.json` 的 `exports`，新增：
```json
"./ui": {
  "types": "./dist/ui/index.d.ts",
  "import": "./dist/ui/index.js"
},
"./app-kit": {
  "types": "./dist/app-kit/index.d.ts",
  "import": "./dist/app-kit/index.js"
}
```

- [ ] **Step 2: 全量构建 + 类型检查 + 测试**

Run（`web/shared`）：
```bash
pnpm build && pnpm typecheck && pnpm test
```
Expected: 构建产出 `dist/ui/index.js`、`dist/app-kit/index.js`、`dist/design-system/tokens.css`；typecheck 无错；测试全绿。

- [ ] **Step 3: 验证子路径可被解析（干净视角）**

Run（`web/shared`）：
```bash
node -e "import('@aiteam/shared/ui').then(m=>console.log('ui:',Object.keys(m)))"
node -e "import('@aiteam/shared/app-kit').then(m=>console.log('kit:',Object.keys(m)))"
```
Expected: 打印 `ui: [ 'cn', 'GlassPanel', 'Button' ]` 与 `kit: [ 'createQueryClient', 'QueryProvider', 'ErrorBoundary' ]`。

- [ ] **Step 4: Commit**

```bash
git add web/shared/package.json
git commit -m "feat(web/shared): 导出 ui 与 app-kit 子路径"
```

---

## Task 10: Tailwind v4 集成确认（agent 端贯通 + 降级）

> 本任务是 spec §3.2 的集成确认：在一个真实消费端跑通 Tailwind v4 + shared token CSS + GlassPanel。若 v4 集成受阻，回退 Tailwind v3 + 共享 JS preset（见"风险"），并据此调整后续端计划。

**Files:**
- Modify: `web/agent/package.json`
- Modify: `web/agent/vite.config.ts`
- Create: `web/agent/src/styles/app.css`
- Create: `web/agent/src/styles/integration.test.tsx`

- [ ] **Step 1: 安装 Tailwind v4**

Run（`web/agent`）：
```bash
cd web/agent
pnpm add -D tailwindcss@^4.0.0 @tailwindcss/vite@^4.0.0
```
Expected: 写入 `web/agent/package.json` 与锁文件。

- [ ] **Step 2: vite 挂 Tailwind 插件**

修改 `web/agent/vite.config.ts`：在 import 增加 `import tailwindcss from "@tailwindcss/vite";`，并把 `plugins: [react()]` 改为 `plugins: [react(), tailwindcss()]`。

- [ ] **Step 3: 建入口 CSS（import tailwind + shared token + 扫描 shared 源）**

创建 `web/agent/src/styles/app.css`：
```css
@import "tailwindcss";
@import "@aiteam/shared/design-system/tokens.css";

/* 让 Tailwind v4 扫描 shared 组件源里的工具类（GlassPanel/Button 等）。 */
@source "../../../shared/src";
```

- [ ] **Step 4: 写集成测试（GlassPanel 渲染 + Button 可用）**

创建 `web/agent/src/styles/integration.test.tsx`：
```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { GlassPanel, Button } from "@aiteam/shared/ui";

describe("shared 地基在 agent 端集成", () => {
  it("GlassPanel 与 Button 可被消费端 import 并渲染", () => {
    render(
      <GlassPanel data-testid="panel">
        <Button>发送</Button>
      </GlassPanel>,
    );
    expect(screen.getByTestId("panel").className).toContain("glass");
    expect(screen.getByRole("button", { name: "发送" })).toBeInTheDocument();
  });
});
```

- [ ] **Step 5: 构建 shared 后跑 agent 类型检查 + 测试 + 构建**

Run（`web`）：
```bash
cd web
pnpm --filter @aiteam/shared run build
pnpm --filter @aiteam/agent run typecheck
pnpm --filter @aiteam/agent run test integration
pnpm --filter @aiteam/agent run build
```
Expected: typecheck 无错；集成测试 PASS；`vite build` 成功（Tailwind 编译生效，token CSS 被打入产物）。

- [ ] **Step 6: Commit**

```bash
git add web/agent/package.json web/agent/vite.config.ts web/agent/src/styles/app.css web/agent/src/styles/integration.test.tsx web/pnpm-lock.yaml
git commit -m "feat(web/agent): Tailwind v4 集成确认——消费 shared token 与组件库"
```

---

## Task 11: 全量 web-ci 等价校验 + 收尾

**Files:** 无（仅校验）

- [ ] **Step 1: 本地跑通 web-ci 等价命令（干净顺序）**

Run（`web`）：
```bash
cd web
pnpm install --frozen-lockfile
pnpm --filter @aiteam/shared run build
pnpm -r typecheck
pnpm -r test
pnpm -r build
```
Expected: 全部成功——与 `.github/workflows/web-ci.yml` 步骤一致（shared 先 build → typecheck → test → build）。

- [ ] **Step 2: 确认未触碰后端与受保护资产**

Run（仓库根）：
```bash
git diff --name-only origin/feature/v1.0.0...HEAD | grep -E "^(server/|app/|\.hermes/)" && echo "VIOLATION" || echo "OK: 仅前端改动"
```
Expected: `OK: 仅前端改动`。

- [ ] **Step 3: 收尾说明**

地基（Phase 1）完成：shared 升级为 React 组件库 + 黑金 token 单一来源（生成 @theme CSS + 玻璃降级）+ app-kit；agent 端 Tailwind v4 集成确认通过。后续 Phase 2（Agent 端页面重写 + AppShell/Workspace 双栏 + 统一骨架）另立计划，复用本计划的组件/构建模式。

---

## 风险与回退

| 风险 | 应对 |
|---|---|
| Tailwind v4 `@source`/`@theme` 在 monorepo 跨包扫描不生效 | Task 10 即暴露；回退 Tailwind v3 + 共享 JS preset（shared 导出 `tailwind-preset.cjs`，各端 `presets:[...]`），token CSS 改由 `:root` 变量注入；其余任务不变 |
| jsdom 下既有 node-env 逻辑测试失败 | Task 1 Step 4 提前验证；若个别用例依赖 node API，单独标注 environment 注释保留 node |
| tsc 不复制 .css 致 tokens.css 缺失 | 已用 Task 3 的 build 脚本由编译产物生成，规避 tsc 资产复制问题 |
| 新依赖致 `--frozen-lockfile` 失败 | 每次加依赖后提交更新的 `web/pnpm-lock.yaml`（Task 1/6/10） |

---

## Self-Review

- **Spec 覆盖**：§3 token 单一来源→Task 2/3；§3.2 Tailwind v4 + 回退→Task 10 + 风险表；§4 组件库（Radix+Tailwind 自有）→Task 4/5/6；§5.4 TanStack Query→Task 7/8；§5.3 app-kit→Task 7/8；§6 性能降级（@supports 实色兜底）→Task 3 + Task 5；保留 testid/role 契约→各组件测试用 role/testid。AppShell/Workspace 与各端骨架迁移属 Phase 2+，本计划范围外（已在"范围"声明）。
- **占位符**：无 TBD/TODO；每个代码步骤含完整代码与确切命令/预期。
- **类型一致**：`buildTokensCss`、`createQueryClient`、`GlassPanel`、`Button`/`ButtonProps`、`QueryProvider`、`ErrorBoundary`、`cn` 命名在定义与引用处一致；exports 子路径与 barrel 导出一致。
