# AI Team test layers

This subtree holds AI Team-specific automated tests.

- `layer0_contracts/`: shared contract and host seam tests that freeze Layer 0 northbound/event/routing behavior.

Follow-on layers should add sibling directories under `tests/aiteam/` so contract and regression coverage stays grouped by implementation layer.
